# 帧同步常量单一来源（与 C++ protocol.hpp 保持一致）
# 修改后请运行: python -m gfootball.frame_sync.schema.generate_config
# 并同步更新 third_party/gfootball_engine/src/frame_sync/protocol.hpp 中的常量

frame_input_timeout_ms: 200
max_predict_ahead_frames: 3
max_frames_without_packet: 5
state_hash_interval_k: 10
