---
description: gfootball_engine 采用 Google C++ Style Guide 风格与约定
globs: third_party/gfootball_engine/**/*.cpp,third_party/gfootball_engine/**/*.hpp,third_party/gfootball_engine/**/*.h
alwaysApply: false
---

# Google C++ Style Guide

- **参考**：[Google C++ Style Guide](https://google.github.io/styleguide/cppguide.html)。引擎新增或大块重写时采用其风格与约定；与现有代码风格或本仓库其它规则冲突时，以本仓库规则为准。
- **命名**：类型（类、结构、类型别名、枚举）用 `PascalCase`（如 `MyClass`）；函数、变量用 `snake_case`（如 `my_function()`、`my_variable`）；常量用 `kConstantName` 或全大写。**Class data members**：小写 + 下划线 + 末尾下划线（如 `name_`、`table_name_`）。现有引擎混用处可保留，新代码尽量统一。
- **格式**：缩进 2 空格；行宽 80 字符（可放宽至 100）；大括号换行风格与 Google 一致（函数/类左大括号不换行，控制语句左大括号换行等，见官方文档）。
- **头文件**：`#include` 顺序：相关头文件、C 库、C++ 库、其它；头文件自包含且可独立编译；优先前置声明减少依赖。
- **注释**：文件头注释、类与函数用途说明；复杂逻辑写“为什么”；用 `//` 单行或块注释，风格与 Google 一致。
- **其它**：优先 `constexpr`/`const`；禁止非平凡静态/全局变量；与 C++ Core Guidelines 及本仓库其它规则并行使用。
