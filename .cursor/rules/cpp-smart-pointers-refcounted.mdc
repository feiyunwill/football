---
description: gfootball_engine 智能指针与 RefCounted 使用约定
globs: third_party/gfootball_engine/**/*.cpp,third_party/gfootball_engine/**/*.hpp,third_party/gfootball_engine/**/*.h
alwaysApply: false
---

# 智能指针与 RefCounted

- **现有约定**：继承自 `RefCounted` 的类使用 `boost::intrusive_ptr`；部分场景使用 `boost::shared_ptr` / `boost::weak_ptr`。
- **新代码**：优先使用 `std::unique_ptr` / `std::shared_ptr`；仅在必须与现有 RefCounted 体系交互时使用 `boost::intrusive_ptr`。
- **改动 RefCounted / intrusive_ptr**：需考虑多线程安全（如 refCount 原子化）与 ABI（与 Python/Boost.Python 的兼容性）；单次改动保持可编译、可测。
