# ms-11.2: 设计完全移除方案

## 基本信息

- **ID**: ms-11.2
- **标题**: 设计完全移除方案
- **阶段**: Phase 11
- **优先级**: 高
- **状态**: COMPLETED

## 目标

设计完全移除 OOP 包装层的方案，实现真正的 ECS 架构。

## 成功条件

- [x] 设计完全移除方案
- [x] 生成设计方案文档

## 实现细节

### 方案概述

1. **Player 系统重构**
   - 在 Player 类中添加 `FillPlayerStateComponent()` 方法
   - 在 PlayerStateSystemDirect 中调用 `FillPlayerStateComponent()`

2. **Humanoid 系统重构**
   - 在 Humanoid 类中添加 `FillHumanoidStateComponent()` 方法
   - 在 HumanoidStateSystemDirect 中调用 `FillHumanoidStateComponent()`

3. **Ball 系统重构**
   - 在 Ball 类中添加 `FillBallPhysicsComponent()` 方法
   - 在 BallPhysicsSystemDirect 中调用 `FillBallPhysicsComponent()`

4. **Team 系统重构**
   - 在 Team 类中添加 `FillTeamStateComponent()` 和 `FillTacticsComponent()` 方法
   - 在 TeamProcessSystemDirect 中调用这些方法

5. **Referee 系统重构**
   - 在 Referee 类中添加 `FillRefereeStateComponent()` 方法
   - 在 RefereeProcessSystemDirect 中调用 `FillRefereeStateComponent()`

6. **MentalImage 系统重构**
   - 在 MentalImage 类中添加 `FillMentalImageComponent()` 方法
   - 在 MentalImageSyncSystemDirect 中调用 `FillMentalImageComponent()`

### 文件变更

| 文件 | 变更 |
|------|------|
| `engine/oop_removal_plan.md` | 新增：OOP 包装层移除方案 |

## 进度

- **开始时间**: 2026-09-02
- **预计完成**: 2026-09-02
- **实际完成**: 2026-09-02
- **完成百分比**: 100%
