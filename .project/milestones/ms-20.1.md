# ms-20.1 — 架构与状态生命周期

类型：milestone

状态由 `python3 .project/quality.py status` 根据当前源码和验收证据计算。

验收条件：

- 所有计划与依赖通过；不以历史完成记录替代当前验收

依赖：ms-19.1

执行顺序：plan-20.1.1、plan-20.1.2

验收检查：architecture_regression

执行：`python3 .project/quality.py run ms-20.1`

实现未就绪、检查失败、证据过期或子任务未通过时，不能完成。
