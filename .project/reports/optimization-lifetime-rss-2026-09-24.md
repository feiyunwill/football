# 重复重启生命周期 RSS 失败与诊断

2026-09-24 诊断终态补充：A 的两次原二进制及三次统计副本、B 的三次精确映射统计副本均退出 0；8 次均完成原 167 条断言。本轮复核全部执行日志哈希。A 的原二进制第二次 final-warm=15,753,216 字节，距原 16 MiB 阈值较近。B 的三个差值为 135,168、-385,024、-749,568 字节，映射 RSS 与原 statm 读数在预热后的记录中相符；不支持简单归因于 RSS 统计不准。活跃分配在统计副本预热后保持 209,469 字节，但不能据此排除其他保留来源。原正式失败仍未关闭，阈值、消毒器和测试流程未放宽。以下保留早期排队记录。

2026-09-24 提交前状态更新：正式 B 已结束，AI 决策通过，架构、输入和战术集成失败，本轮公共 UDP 完整运行未执行。控制记录和输入周期候选均因前置依赖失败结束，未执行测试。内存诊断 A、B 已完成采集，未关闭原架构失败。独立 UDP 探针迁移报告通过 8 场对局和 16 次独立回放；真实客户端探针迁移报告通过 4 场对局和 8 次独立回放。上述探针仍在私有候选目录，尚未接入正式检查器，不能视为完整战术门禁或产品验收通过。以下保留阶段历史记录。

归属：ms-20.1 → plan-20.1.2 → task-20.1.2.2。正式架构门禁未通过，原因尚未确定。

## 已确认的失败

正式 architecture_regression 于 1790212606.5548894 开始，运行 911.18 秒后退出 1。日志 architecture_regression-1790212606554937767.log 的 SHA-256 为 06623a4e0fe545d9117c64803029430ae68bc4dfd3f92e69cec5ff69cfa79118。失败来自原有 engine_lifetime_contract 的重复重启 RSS 断言：12 次重启，以 cycle=3 为预热基准，最后一次内存不得高出 16 MiB。原始失败没有输出这两个内存数值，因此不能据此断言是对象泄漏或分配器缓存。

此前通过的加载、取消、AI、两个种子的无窗口/渲染与仿真检查，只能证明对应子项；不能抵消此失败。历史 loading-transport-gate-adoption-20260924-b 曾记录 warm=325001216、final=327585792 字节，但历史通过不能替代当前门禁。

## 已排队的诊断

独立目录 native-lifetime-rss-diagnosis-20260924-a 保存原始测试源、只增加统计输出的副本、原编译/链接命令及失败证据。保持全部测试、12 次循环、cycle=3 基准和 16 MiB 限制。完整 Debug 使用 -g、ASan/UBSan、禁止恢复、保留栈帧；环境与失败门禁一致，quarantine_size_mb=16、泄漏检测开启、无抑制。

队列等待 B 驱动及两个依赖候选全部终止。随后串行执行两次原二进制和三次统计副本，逐次保存退出状态与日志。副本记录 RSS、ASan 当前已分配字节、堆总量、空闲与已解除映射字节；不主动清理分配器，不调高阈值，不将后续单次通过覆盖原失败。二进制和核心库哈希在排队及每条命令后复核。

Linux owner PID2734622/start_ticks69675587，1790213671.616475 启动，目前尚未执行编译或运行。diagnostic_execution_complete 仅表示证据采集完成，architecture_accepted 与 product_acceptance 保持 false。

## 后续判断与修复

先用逐次活跃分配和堆/RSS 走势定位保留来源，再选择对应修复；现阶段不宣称根因或修复完成。修复必须在新的独立阶段验证，原有失败和输入保持不可变。正式源码仍冻结在多席位 B 的 1128 项清单，全部当前任务结束后才可安排接入与完整架构重验。

代码图 Verify 已定位 main、ResidentBytes、Start、GameEnv.close；相关文件覆盖无记录缺口。main 的图追踪包含启发式错配，因此释放路径以精确源码为依据，不将错误边当作实际调用链。GameEnv.close 确有 quit_game、delete context 和 TLS 恢复，但这不足以证明所有内存都已释放。
