# 训练检查点文件 I/O 候选

2026-09-24：从现有 checkpoint.hpp 提取的原始文件写入段，在真实 Linux /dev/full 写入失败后仍返回成功，完整 Debug ASan/UBSan 下已确定性复现。该复现只隔离文件写入边界，不模拟或验证 RLtools 序列化。

候选从现有回放文件实现提取共享原子发布层，保留回放接口，并增加有界检查点文件读写。写入检查每一步错误；提交前失败保留旧文件，提交后目录同步错误明确报告 committed。读取拒绝空、超限、非普通文件及读取不完整；归档默认上限为 64 MiB，可在文件 API 中显式指定。拒绝覆盖符号链接或特殊文件。

12 项新增文件测试包括真实 RLIMIT_FSIZE 写入失败、短写/中断/磁盘不足、文件及目录同步、关闭、发布、超限读取、部分读取丢弃、并发原子替换。连同原有 10 项回放文件、16 项回放目录测试，Release 和完整 Debug ASan/UBSan 各 38 项全部通过。

证据：rl-checkpoint-write-error-20260924-a 和 rl-checkpoint-file-candidate-20260924-a。候选尚未接入正式源码。仓库声明的 RLtools 子模块仍为空，完整 checkpoint.hpp 模板未实例化；序列化本身的内存上限、损坏或维度不匹配时训练状态原子恢复、完整训练生命周期均未验证。不能据此提升训练或产品级状态。
