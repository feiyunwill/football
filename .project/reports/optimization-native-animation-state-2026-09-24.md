# 逻辑动画通知与状态批量写入

归属 ms-23.1 → plan-23.1.1 → task-23.1.1.2，关联性能、渲染和网络。候选尚未采纳，产品验收未通过。

mirror D已通过完整Debug的镜像/缓存契约、双向1500帧旧/新快照与两个种子恢复。客户端步中位从缓存B的27.745ms降到16.706ms，但实际TCP仍只完成16次呈现后输入窗口耗尽，4228客户端步对5270服务端步；6900输入和37日志核验。客户端每帧摘要仍约6.261ms，逻辑动画Fetch每物理步2.023ms，服务端1.172ms。数据包含观测开销，不能相加或称产品性能达标。

独立真实EnvState对照已完成。旧/新实现各24832断言，8192组向量、四元数、角度和含零字节字符串，共360448字节，与独立字段预期及彼此完全一致。读取截断/非有限数/非法角度标志拒绝，参考比较、负零位级差异、最大容量错误位置及GetState后续写均通过。9轮各20次序列化中位345.889ms→104.618ms；这是完整Debug组件测量，不能替代整机验收。6911输入及6日志核验。

普通保存路径按Vector3.coords、Quaternion.elements连续数组写入；radian按angle字段、规范标志和三个显式零字节写入；字符串保留长度前缀和原始字节。不复制对象padding，读取和参考比较保持原逐字段实现。容量不足以容纳完整字段时仍用原逐字段路径，保留原部分写入与错误偏移。

逻辑动画Apply原本以updateSpatial=false写入关节，FetchPutBuffers末尾再递归刷新。新候选保留递归缓存失效与非图形观察者，只排除临时逻辑骨架的Graphics通知。真实显示的fullbodyNode和头发仍由Put/PutInterpolated发布。真实球员动画验证已复现旧通知反例；旧/新各16项断言通过，非图形观察者与碰撞派生值正确，最终显示姿态摘要同为7577643041246268683。组合候选的24832项批量写入、19项镜像及22项缓存断言也已通过；完整窗口与Release验收仍待完成。

新阶段native-animation-state-engine-20260924-a按原完整Debug与Release要求执行组件、跨版本快照、恢复、原TCP/UDP窗口及独立旧核心回放；不得放宽30秒启动、1000次呈现、250ms整个进程退出、300秒独立回放或sanitizer配置。

永久验收准备树已扩展到40文件：控制入口覆盖12普通客户端、6恢复故障客户端、4窗口和22独立回放；状态入口覆盖写入/缓存/镜像/批量写入/动画、两个种子恢复和双向各1500帧Release/Debug快照续跑。两个入口仅完成语法解析，注册项ready=false，尚未执行或正式接入。Host菜单、完整弱网/恢复矩阵、原UDP中立帧与RSS因果、性能基线、目标硬件和训练打包等仍未完成。
