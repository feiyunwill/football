2026-09-24 增量观测终态与provider组件：增量日志11单测、270600记录等价通过，8499输入/3日志核验。相同产品程序的窗口未再复现客户端输入窗口耗尽，但仅22呈现，最终Private GUI Host deadline expired；客户端13542确认帧/1354哈希核验，服务端13613步，客户端退出0/服务端1；原1000呈现门槛未过。provider候选完整Debug/Release各2054断言通过，包含提供者失败/错误长度、空输入、失败恢复和并发一致性，373输入/4日志核验。新native-state-hash-engine-20260924-a正在重编译实际哈希使用者、复验组件/跨版本快照/恢复/窗口/独立回放，之后完整Release；121份核心编译依赖文件确认新哈希头不属于复用核心，正式源未采纳。以下为历史记录。

# 状态哈希实现的等价与性能对照

归属 ms-23.1 → plan-23.1.1 → task-23.1.1.2，涉及性能、网络和依赖架构。尚未修改正式源码或取得整机验收。

MakeGameEnvCallbacks先序列化canonical状态，再调用ComputeStateHash。后者当前用项目内逐轮SHA-256实现，返回摘要前8字节的小端整数。图谱验证与精确源码确认该语义；项目的原生恢复已使用OpenSSL 3。

native-state-hash-comparison-20260924-a使用现有OpenSSL EVP_Q_digest，与旧实现核对空串/abc已知向量、27种长度、3种二进制内容、分块输入、完整32字节摘要及协议前8字节。完整Debug调用端（ASan/UBSan、-g、无优化）和Release各2048项断言通过，跨构建校验和相同。OpenSSL为系统已安装依赖，未更改编译器或安装环境。

固定87132字节输入，9批各100次，交替前后执行顺序，中位耗时：

| 构建 | 原实现 | OpenSSL |
| --- | ---: | ---: |
| 完整Debug调用端 | 277.403528ms | 2.586684ms |
| Release | 25.408871ms | 2.343117ms |

358个固定输入与4份命令日志核验无变化。此测量只代表哈希组件，不代表真实比赛帧耗时或最终网络质量。

后续provider候选保留现有SHA-256语义、字节序和校验频率，提供者失败或摘要长度异常时抛错；CMake显式传递现有Crypto依赖。已准备空输入/非法指针、提供者错误、后续调用与并发一致性检查，尚未执行。仍须实际产品、独立旧核心回放、框架及完整Debug/Release门禁证明可采纳。

当前另一个串行阶段native-trace-reader-validation-20260924-a在相同产品程序上验证增量日志观测，11项边界测试与270600条旧记录等价已通过，窗口结果仍待终态。不能将降低观测开销当作改变产品验收门槛，也不能提前归因全部追帧失败。
