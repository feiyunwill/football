# 原生战术控制边界回归与正式修复

2026-09-24 终态补充：本轮战术修复在 1134 项源码版本上通过完整 tactical_integration（32 项结果、161,819 条断言、19 项测试端自检）及 native_boundary。Release/完整 Debug 的 6 个真实控制场景均通过，24 次跨构建独立回放完成。记录哈希已复核。随后接入输入调度修复，源码变为 1135 项，正在重新运行上述门禁；不能把前一版本证据作为最新源码的验收结果。以下保留阶段历史。

归属：ms-25.1 → plan-25.1.1 → task-25.1.1.1；关联 ms-23.1 的会话正确性。本轮改动已接入，完整验收正在执行，未提升任务或产品完成状态。早期私有阶段 plan.json 将 task 记为决策稳定性的 task-25.1.1.2；本报告更正归属，原始执行输入保留不变。

## 失败证据与修复

原正式 B 的战术集成在旧 UDP 测试端握手处失败。测试端已迁移到当前 FNDU 传输及 FNRC 加载流程，并保持原接管、控制边界和真实客户端验收条件。独立 UDP 探针的 8 场对局、16 次跨构建回放，以及真实客户端探针的 4 场对局、8 次跨构建回放全部通过；本轮已逐条复核 30 条执行命令的退出结果与日志哈希，输入哈希一致。

控制测试迁移后暴露出生产回归：native-tactics-control-probe-migration-20260924-a 中，分片通知与非法席位场景通过，但非法帧通知被实际客户端接受，客户端以 confirmed=3、failed=0、exit=0 结束。原测试要求 failed=1、exit=1；失败日志与候选输入已保留。

共享客户端 integrated_client.cpp 的接管/归还通知分支只读取席位，没有读取或检查帧号。旧 integrated_client_udp.cpp 曾校验 frame == received_authority_count_，迁移共享实现时遗漏了这个条件。正式修复统一调用原有完整记录解码器，并在 native_product 模式中校验通知帧等于已接收的权威边界，验证通过后才更新 AI 席位状态。比较接收边界可避免将渲染或仿真追赶延迟误当作非法消息。

## 验收范围

正式目录 native-tactical-boundary-adoption-20260924-a 保存原始文件、11 项接入文件哈希、前置报告、1134 项当前源码清单及执行输入。所有前置进程已终止，并取得正式门禁锁后才更新源码。

依次运行完整 tactical_integration、native_boundary。战术门禁仍包含真实比赛状态、TCP/UDP 两队接管、真实客户端存活和控制边界、两种构建中的服务器及客户端独立回放。新增的 19 项独立测试端反例检查成为该门禁的固定前置步骤。完整 Debug 的 -g、ASan/UBSan、禁止恢复、保留栈帧及无 -O1 配置另行核验。

graph Verify generation=2026-09-24T01:54:49Z。已核对共享解析器精确源码和覆盖；共享客户端 1326 行及旧 UDP 客户端 617 行的解析缺口已直读。测试候选位于排除目录，按精确路径读取并校验哈希。

## 尚未关闭

完整战术与边界验收尚未取得终态。架构原 RSS 超限、暂停恢复后的 UDP 输入空帧、原生菜单与暂停屏障、重连及弱网全场景、硬件渲染与产品延迟、训练和包装等仍未通过。此修复不将任何剩余范围缩小为组件通过。
