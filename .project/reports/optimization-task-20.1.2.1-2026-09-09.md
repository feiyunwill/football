# task-20.1.2.1 逻辑、渲染与快照契约验收

2026-09-09：`python3 .project/quality.py run task-20.1.2.1` 和 `verify task-20.1.2.1` 通过，下一任务为架构集成回归。整体优化目标仍未完成。

实现：快照增加版本、载荷长度、CRC32 和大小上限；解析前检查容器数量、引用索引、布尔、枚举及有限浮点。随机数改为 MT19937 规范状态字，避免恢复对象内部内存。恢复失败时退出镜像并回滚完整比赛备份，保留原帧及其未来轨迹；历史感知对象使用临时副本解码。实际渲染不改变逻辑帧或摘要。

真实引擎验收共 3568 条断言，两种子、双向球队、有效 CRC 包装的深层损坏载荷、完整事务恢复、继续运行和重演；两个 320×180 EGL 对照流程各 92 条断言，逐帧比较有图形/无头逻辑，重复渲染并跨环境传递快照。

全部前置检查重新通过：29 验收工具测试；原生核心/绑定边界及移动打包加载；421 C++ 和 165 Python；两种子各两独立进程 1000 帧与快照重演；8190 状态所有权断言；378 生命周期、独立 LSan、双 EGL、启动失败恢复和 Python 绑定断言。无跳过。

规范 RNG 表示改变了摘要字节，当前 seed42 hash `606593fad9c78ea9`、seed43 `a8c11d6b25d70198`；独立进程及快照重演一致。旧的无版本快照被拒绝；载荷当前针对 Linux little-endian ABI。CRC32 用于检测损坏，不提供认证。若备份恢复遭遇不可恢复内部错误，环境关闭并明确报告。

正式日志位于 `.project/optimization/evidence/`：

- `quality_selftest-1788928627690256431.log`
- `native_boundary-1788928629110312373.log`
- `framework_regression-1788928878688509732.log`
- `state_ownership-1788928906816200210.log`
- `environment_lifetime-1788928911237124910.log`
- `simulation_contract-1788928935710184808.log`

对应 JSON 保存源码、环境和工具指纹。CI 已添加同一检查，尚未远程执行。实际渲染器是 llvmpipe，尚不能证明目标 GPU 帧率；完整 ASan/UBSan 和生命周期集成继续由 task-20.1.2.2 验收。
