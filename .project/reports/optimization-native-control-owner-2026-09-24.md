2026-09-24 终态更新：render-quit A 已 exit 1。Release 的单元、真实 TCP/UDP 窗口及 15322 帧独立回放通过；完整 Debug 的 10 项取消回归通过，但实际 TCP 窗口未在原 30 秒内开始比赛呈现，报 Actual graphical startup deadline，客户端与服务端均正常退出。2739 项固定输入和 28 份命令日志已核验。随后仅增加分段计时的 native-control-sanitized-starvation-diagnostic-20260924-a 也已 exit 1，复现同一启动超时；其分段数据仍待分析。两阶段均已终止，未通过双构建图形验收，候选尚未采纳。以下为历史记录。

2026-09-24 Release 图形验证通过：render-quit A 的 TCP/UDP 各完成 1003/1002 次实际呈现，退出为 73.241/105.959ms；正在绘制的帧分别在 Q 后 6.121/21.285ms 取消，均无交换且逻辑哈希不变，真实全局暂停/恢复标题与输入清理通过。7825+7497=15322 帧全部独立回放通过，15 份 Release 命令日志哈希已复核。完整 Debug/ASan/UBSan 当前执行中；正式源码未采纳，整体图形和产品验收仍未完成。以下为历史记录。

2026-09-24 渲染退出候选：long B 的两次 TCP 和一次 UDP 长程诊断已完成，退出约 136.885/137.710/168.543ms，全部独立逐帧回放通过，2724 项输入与 10 份命令日志哈希核验。原 338.645ms 尚无完整因果解释，仍保留失败。现已实现私有 native-control-render-quit-20260924-a：退出信号在既有渲染检查点中断当前未呈现帧，随后照常保存回放、关闭连接与清理资源，并显示全局暂停状态。Release 10 项取消/异常/作用域/线程回归已通过，双构建真实窗口及原 250ms/1000 呈现门禁仍执行中，不能据此认定产品通过。以下为历史记录。

2026-09-24 后续长程诊断：long A 已终止 exit 0，Release/TCP 8803 权威帧、1003 次实际呈现、独立逐帧回放通过，退出 198.994ms；阶段时间戳显示回放保存 50.040ms，环境关闭 24.944ms。2719 项输入和 6 份命令日志哈希已复核。该次未复现原 338.645ms，不能关闭原失败。long B 正在串行运行两次 TCP 与一次 UDP 的原 1000 呈现规模诊断，并细分 fsync 耗时；无产品行为修改。以下为历史记录。

2026-09-24 真实窗口发现失败：GUI A 的 Release/TCP 已到 1003 次实际呈现、8505 确认帧，暂停中 Q 退出实测 338.645ms，超过原 250ms 门槛，阶段 exit 1；原失败和全部输入/日志均保留。单独短程退出诊断的 TCP/UDP 分别约 163/187ms，事件读取约 1.4/4.0ms、资源清理约 20ms，回放 657/611 帧通过，但仅有各 101 次呈现，不能替代原长程验收或关闭失败。当前正在 native-control-quit-long-diagnostic-20260924-a，以原 1000 次呈现规模加入退出阶段计时；无产品行为修复或正式采纳。以下为历史记录。

2026-09-24 更新：recovery-refresh B 已终止且 exit 0，Release/full Debug 各 51 项回归、12 场普通客户端及 6 场实际断线/过期快照恢复全部通过；18 次独立回放包含每个恢复检查点及全部保存帧。1594 项固定输入和 74 份命令日志哈希已复核。当前 native-control-gui-20260924-a 已进入串行编译，准备两种构建的真实 TCP/UDP 窗口验收，尚无图形通过结论。正式源码未采纳，Host 产品菜单及完整弱网/性能矩阵仍未完成。以下保留历史记录。

2026-09-24 后续：refresh A 在独立检查点回放工具链接时失败（缺少 recovery_snapshot_digest 所在摘要库），51 项 Release 回归已通过，实际场景未运行；22 份命令日志已核验并保留。新 refresh B 只补齐该工具的固定恢复库与 libcrypto 依赖。B 的 Release 已完成全部 51 项测试、6 个普通客户端、3 个真实故障注入恢复客户端及全部独立回放，37 份 Release 命令日志哈希已复核。完整 Debug 尚在执行，不作为两种构建全部通过。正式源码仍未改变。

2026-09-24 更新：client-owner A 已全部通过，两种构建各 48 项回归、12 个实际客户端、24 次暂停/恢复及 12 次独立回放（1200 帧）；1578 项输入和 54 份命令日志哈希已复核。当前继续 native-control-recovery-refresh-20260924-a：补齐真实断线后旧快照跨周期的有界自动重试与旧流隔离，尚未取得完整结果。正式源码未变，详见 optimization-native-control-recovery-refresh-2026-09-24.md。以下为历史记录。

2026-09-24 后续核验：frame-owner B 已终止，exit 0；Release/full Debug 各 24 项控制测试、13 项旧服务端测试及共 12 个实际客户端会话全部通过，34 份命令日志和全部固定输入哈希已核对。client-owner A 已开始，当前正在执行原服务端/模拟回归及实际客户端编译，尚无完整暂停闭环结论。正式 1138 项源码仍与固定清单一致。下方运行状态为历史记录。

# 原生暂停：服务端屏障与客户端帧线程接入

2026-09-24，归属 ms-23.1 → plan-23.1.1 → task-23.1.1.2，关联输入手感与预测回退。正式分支最近提交 44f3ad7；以下实现均为私有候选，未采纳正式源码。

## 已核验结果

控制会话 integration A 的 Release 通过 10 项新测试、13 项旧服务端测试和三入口共 6 个实际客户端。完整 Debug 在测试断言绑定 packed SlotInput 浮点成员引用时触发 UBSan，保留原失败。integration B 仅将这些断言改为先取标量值；Release/full Debug 均通过 10+13 项测试，TCP/UDP/public UDP 共 6 对服务和 12 个实际客户端，均达到 100 确认帧、10 次哈希，正常退出并生成回放。34 份命令日志 SHA-256 已独立核对。该结果仅证明协议接入，不证明暂停功能。

服务端 frame-owner A 新增本地 Host request_paused API，网络 Join 无权直接驱动状态。唯一帧线程在完成边界计算哈希、推进 epoch、清空旧输入，并分别等待 Paused/Resuming ACK；重复旧 ACK 不清除新屏障，未确认者按原有界期限断开，暂停仍处理心跳/快照/恢复，运行时重建 20ms 帧时限。恢复快照处于 Resuming 而全局已经 Running 时，校验同 epoch、原边界与原哈希，并确保 Running 通知先于后续权威帧。周期编码增加固定栈缓冲接口。

frame-owner A 的 24 项测试中 23 项通过；失效快照 Ready 已收到拒绝，但测试立即断言连接数，没有等待既有异步拒绝排空。新 B 保留拒绝类型、原超时与全部断言，仅等待关闭后检查；它重新执行两种构建的 24 项控制/报文测试、13 项旧服务端测试和真实入口会话。结果以对应 results/exit 为准，禁止用 A 的 23 项替代完整通过。

## 客户端实际路径

client-owner A 已准备并串行排在 frame-owner B 全部通过之后：实际 integrated_client 接收有界控制队列，网络接收立即限制输入，唯一游戏帧线程消费至完整权威边界、回退多余预测、核对引擎哈希后 ACK；恢复运行重新建立输入发布历史，并要求松开后重新按键。FrameSimulation 保留已确认回放与哈希，仅丢弃边界之后预测。快照 Ready 使用原快照控制标签，允许已收到同周期 Running 提交的接收路径。

该阶段每种构建保留 24 项服务端控制测试和 13 项旧测试，另增加 11 项模拟测试（原 6 项加 5 项预测边界回归）。基于真实 GameEnv 服务入口的私有 Host 夹具，在每个实际客户端会话中驱动两次暂停/恢复；检查客户端逐阶段遥测、服务端暂停期间帧不前进，并在独立进程逐帧重放真实客户端记录、比较全部哈希，保留 300 秒单次回放上限。使用原编译配方与完整 Debug ASan/UBSan，不增加 O1，不改变环境或安装依赖。

## 未完成

客户端候选尚未完成编译/执行，真实暂停窗口、断线重连全流程、失效快照周期的可恢复重试、Host 产品菜单、长期网络条件和容量性能仍待完成。当前不宣称完整产品暂停通过。原始 UDP 中立帧根因、架构 RSS 失败、受保护性能基线与渲染硬件等缺口继续保留。所有已排队输入不可修改；修正必须另建阶段，构建与测试严格串行。

证据目录：native-control-session-integration-20260924-b、native-control-frame-owner-20260924-a/b、native-control-client-owner-20260924-a；当前句柄快照见 native-control-owner-progress-20260924-a/owners.json。
