# 资源文件读取与 ASE 构建开销

2026-09-22 最新终态：当前完整框架门禁因 Python 最大快照 UDP 恢复失败而结束，后续集成/恢复未启动。文件读取原生回归仍通过，整体验收未通过；网络调度候选尚未采用。见[失败与复现](optimization-python-udp-snapshot-2026-09-22.md)。

以下保留此前阶段记录。

2026-09-22 **正式接入，完整门禁待终态**：文件分块读取及 14 项永久原生回归已写入正式源码，CMake、源文件列表和框架门禁同步登记，共 5 个文件；源码清单由 1089 项变为 1090 项。旧函数按日期注释保留，原源码与修改差异已归档。动画按行读取候选仍未采用。

接入前完成两种构建各 14 项/39 条断言的永久契约预检，以及 24 次实际、无插桩、交替原/新顺序的启动。所有初始状态哈希与先前通过完整比赛对照的核心一致。

| 构建/种子 | 3 对启动 CPU 比值的中位数（新/旧） |
| --- | ---: |
| Release 42 | 1.0412 |
| Release 43 | 0.5436 |
| 完整 Debug 42 | 0.3458 |
| 完整 Debug 43 | 0.3167 |

6 次 Debug 候选启动均在 30 秒参考值以内（6.198–22.954 秒），每对 CPU 均低于原核心；原核心这一轮最大约 67.827 秒。Release 存在波动，种子 42 的 CPU 配对中位数略高，不能宣称所有构建都有稳定统计收益。本次接入依据是完整资源与轨迹等价、去除逐字符读取开销，以及 Debug 的重复启动改善；原正式性能统计要求仍未满足本次改动的验收条件。

完整框架复验已串行启动：先归档现有核心、受影响对象、库、可执行文件和正式测试证据，再重建 Release / 完整 ASan+UBSan 产品，执行 10 项 ASE 与 14 项文件读取回归，最后运行原完整 C++/Python/独立比赛与快照检查。当前观察时点 Release 产品及两类原生契约通过，Debug 产品重建后继续原生契约。不能将旧 1089 项源码的完整框架通过记录当成当前 1090 项的通过记录。

[接入决定与边界](../optimization/benchmarks/native-buffered-file-adoption-20260922-a/decision.json)、[修改清单](../optimization/benchmarks/native-buffered-file-adoption-20260922-a/changes-applied.json)、[重复启动完整报告](../optimization/benchmarks/native-buffered-file-startup-20260922-a/report.json)、[本轮证据摘要](../optimization/benchmarks/native-buffered-file-adoption-evidence-20260922-a/summary.json)、[完整门禁计划](../optimization/benchmarks/native-buffered-file-gates-20260922-a/plan.json)。

门禁重建会改变临时目录中的核心文件，旧阶段引用的绝对临时路径不再代表原身份；以完整门禁的 archives.json 及其中原产物摘要查找历史版本。保护基线没有替换。实际 UDP 恢复矩阵、30 秒服务端启动、真实窗口 250ms 退出及整体产品验收仍须复验，不提升相关状态。

以下保留接入前记录。

2026-09-22 功能候选最终通过：退出码 0，22 条编译/链接/运行命令均成功；全部 461 个资源逐字节相同，双构建 13 项边界检查通过。8 次真实引擎运行均完成 1000 帧及 500 帧快照回放，4 组原/新对照各 1001 个状态哈希一致。完整 Debug sanitizer、输入、产物及日志摘要复核通过，正式源码仍未修改。

| Debug 种子 | 原核心墙钟/CPU 秒 | 候选墙钟/CPU 秒 |
| --- | ---: | ---: |
| 42 | 17.875 / 17.817 | 12.419 / 11.876 |
| 43 | 35.854 / 34.682 | 4.756 / 4.681 |

本轮两组实际启动均改善，但幅度不同，尚不能据此宣称统计收益或实际服务端门禁已通过。后续 14 项永久契约预检及 24 次交替重复启动已开始，沿用同一候选和当前原核心，不采用过期基线。见[最终报告与独立复核](../optimization/benchmarks/native-buffered-file-validation-evidence-20260922-a/report.json)、[重复测量观察](../optimization/benchmarks/native-buffered-file-validation-evidence-20260922-a/startup-observation.json)。正式接入仍待下一阶段结果。

以下为此前观察记录。

2026-09-22 后续验证进度：共用文件读取候选已通过 Release / Debug 全部 461 个资源、13 项边界、两轮交替字节比较，各 5557 条断言。读取 CPU 总计 Release 为旧 0.68601 秒、新 0.45161 秒；Debug 为旧 70.4891 秒、新 1.04716 秒。这仅是固定资源集读取环节的局部结果，不能换算为整个产品提速比例。

Release 两个种子的原/新核心均通过 1000 帧与 500 帧快照回放。Debug 种子 42 两者也通过，原/新启动墙钟 17.875/12.419 秒、线程 CPU 17.817/11.876 秒；种子 43 原核心通过，启动 35.854 秒，候选仍在执行，完整结果待终态。

已准备五个文件的正式接入副本，包括精确候选、14 项永久契约及框架门禁登记，但尚未修改正式源码。永久契约增加非重复块边界模式、资源替换后重新读取和临时资源清理。另已排队：在功能候选完整通过后，串行执行双构建永久契约预检，以及每种子/构建三组交替原/新顺序的无插桩启动测量，共 24 次，核对已验证初始哈希并分别记录 CPU/墙钟。若前置失败则后续停止，不能据排队视为通过。

[准备证据](../optimization/benchmarks/native-buffered-file-preparation-evidence-20260922-a/observations.json)、[接入副本计划](../optimization/benchmarks/native-buffered-file-adoption-20260922-a/plan.json)、[重复启动计划](../optimization/benchmarks/native-buffered-file-startup-20260922-a/plan.json)。原 30 秒实际服务端启动、250ms 取消及正式性能统计门槛不变。图谱中的 CMake/框架门禁元数据已滞后，已用当前全文及摘要核对补证；私有新文件使用实际源码和执行依赖补证。

以下保留前一观察时点的记录。

归属 **里程碑 21.1 → 计划 21.1.1 → 任务 21.1.1.2**。当前正式源码保持 1089 项，本轮诊断和候选均未采用；原 30 秒启动、250ms 取消、完整 Debug 与 sanitizer 要求不变。

## 动画读取候选的最终结果

候选 A 已以退出码 0 完成。Release / Debug 各通过全部 293 个动画文件、9 项文本边界、4 轮交替逐行比较及 8216 条断言。两种构建各有两个种子的原核心/候选核心对照，共 8 次真实引擎运行，每次 1000 帧与快照恢复后的 500 帧重放，所有状态哈希一致；sanitizer 与清理检查通过。

局部读取 CPU：Release 旧 0.02126 秒、新 0.00685 秒；Debug 旧 1.65211 秒、新 0.08132 秒。整体启动却没有相应改善证据：

| Debug 种子 | 原核心启动秒 | 候选启动秒 |
| --- | ---: | ---: |
| 42 | 30.568 | 34.520 |
| 43 | 14.040 | 55.254 |

本轮结果不足以支持整体性能收益，暂不合入动画候选。没有把语义等价或局部微基准当成产品启动通过。见[动画最终报告](../optimization/benchmarks/native-animation-load-20260922-a/report.json)。

## ASE 构建的进一步分段

此前外层 ASE 构建计时包含材质资源加载。本轮保留所有原计算，在私有副本中区分顶点、三角形、法线、UV、切线打包、释放、四种材质贴图及网格发布，并记录墙钟和线程 CPU。

共 12 次实际启动：两种构建各两个种子的未插桩对照，加每种子两次插桩运行。所有初始状态与同配置当前原核心一致，全部命令退出 0；源码、输入、产物、日志摘要复核通过。

| Debug 运行 | 全启动秒 | 材质 CPU 秒 | 网格阶段 CPU 秒 | 材质占这些分段比例 |
| --- | ---: | ---: | ---: | ---: |
| 42-0 | 13.819 | 2.102 | 0.102 | 95.37% |
| 43-0 | 16.528 | 2.156 | 0.203 | 91.40% |
| 42-1 | 14.671 | 2.596 | 0.082 | 96.93% |
| 43-1 | 11.331 | 1.887 | 0.076 | 96.14% |

比例仅针对本轮已划分的 ASE 网格/材质子阶段，不能解读为整个启动的比例。漫反射与法线贴图加载占主导，当前应优先优化共用资源读取，不能因外层名称为“构建”就假定瓶颈是三角形运算。父子计时不重复累加。

这些是诊断运行，不替代实际 UDP 服务器原有启动失败记录。见[分段完整报告](../optimization/benchmarks/native-ase-build-profile-20260922-a/report.json)及[独立复核摘要](../optimization/benchmarks/native-ase-build-file-evidence-20260922-a/validated-summary.json)。

## GetFile 分块读取候选

GetFile 被 ASE、图片、文本配置和顶点颜色路径调用。原实现通过字符迭代器构造整段字符串；新私有候选在同一个文本模式 streambuf 上以 64 KiB 分块读取，保留字符串返回类型、缺失文件结果和底层读取异常。原代码按日期注释保留，无缓存、资产缩减、解码跳过或渲染降级。

验证计划覆盖全部 **461 个资源、90,974,146 字节**：88 张 BMP、24 个 ASE、293 个动画以及 shader、字体和 object 文件。两轮交替新旧读取逐字节比较，并检查 13 项边界，包括嵌入 NUL、全部字节值、64 KiB 边界、缺失路径和目录读取异常。随后进行 Release / 完整 ASan+UBSan 下两个种子的实际引擎原/新对照，共 8 次 1000 帧及 500 帧快照回放，并记录启动墙钟与线程 CPU。

候选已在上一步诊断终态后串行启动，结果待完成。编译使用当前正式对象，精确替换 file.cpp 对象；每个运行核对实际映射的核心路径。原 300 秒整段引擎验证期限保留，不能据其通过声称启动满足 30 秒。

见[候选计划](../optimization/benchmarks/native-buffered-file-read-20260922-a/plan.json)及[本轮观察快照](../optimization/benchmarks/native-ase-build-file-evidence-20260922-a/buffered-observation.json)，最终以候选自己的 exit/report 为准。

## 后续与限制

先核验分块读取的资源语义、运行轨迹和实际 CPU 收益，再决定正式接入及永久门禁。实际恢复矩阵、加载断线、快照中断、旧代际数据、真实窗口输入退出、长期弱网及产品级总体指标仍需完成，不提升任务状态。

正式文件的图谱覆盖 generation 为 2026-09-22T02:15:16Z，所用路径无已记录缺口。新私有路径尚未收录，已读取实际源码、差异并记录编译依赖补证。修改前归档全部 13 份当前文档。
