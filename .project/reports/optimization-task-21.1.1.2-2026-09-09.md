# task-21.1.1.2 查询与分配优化验收

**已通过正式验收；plan-21.1.1 已验证。下一任务为 task-21.1.2.1 网络与回滚内存预算。**

## 审查和实现

- 修复 const World 缺失组件池时的空指针访问；修复三种不同组件类型的 ForEach 无法编译的问题。
- Query 每次执行缓存类型池，先选最小有序候选池；缺失类型不创建空池。保留候选快照与结构变更后重新取值的行为，覆盖清空、删除、增长、顺序和异构三组件。
- 实际比赛的玩家同步、物理同步、碰撞缓存与完整 ECS 同步在每次调用中取得类型池；球队实体只读搜索采用有序视图。
- 新增动画只读属性 API，保留原有按值 API；透明查找避免临时键和值。短向量文本采用栈缓冲，仍使用 atof 和原有分隔符规则，长文本保留动态字符串路径。
- 自检源码指纹补入其实际导入的 .project/checks/*.py；原先仅有 quality.py、tests/*_test.py、program.json 和 scripts/*.sh。更新后重新执行自检并验证完整计划。

## 正式性能结果

Intel Core Ultra 9 285H / WSL2 Linux / GCC 16.2.1，Release -O3 -DNDEBUG、C++23、CPU 0。每种种子固定 15 对相邻独立进程，交替基线/候选顺序；60 个进程共 120000 个稳定帧。每进程先预热 200 帧，测量 2000 帧，再从预热快照重演 2000 帧。未锁定主机频率，以下是本机本批次统计，不代表其他硬件。

| 种子 | CPU 配对几何比值（95% 区间） | CPU 降幅 | 配对 p99 比值中位数 | 分配次数：原始→优化 | 请求字节：原始→优化 |
|---|---:|---:|---:|---:|---:|
| 42 | 0.965379（0.950871–0.977228） | 3.46% | 0.963890 | 1432072 → 1219562（减少 14.8%） | 121060944 → 111779552 |
| 43 | 0.968463（0.954180–0.981856） | 3.15% | 0.971893 | 1776524 → 1210162（减少 31.9%） | 148724900 → 118467089 |

两种种子的 CPU 95% 区间上界均小于 1，wall time 几何均值均改善，配对 p99 中位数均未超过 1.05 上限。完整同步 full_cache 在每种种子的 20000 次调用中，分配由 40000 次/640000 字节降至零。分配数字来自另外四个诊断进程，代表基准线程的分配调用与请求字节，不代表在用堆大小，也不计入上述耗时。

每个进程核对输入、预热状态、21 个状态检查点、全部 2000 个比赛进行标志和最终状态，并进行快照重演。原始输入哈希 b7f3bcf2014e649e；最终状态种子 42=177cf7205a3272f3、43=39ceb7b90729ca35，均与原始基线一致。

## 验证

- 性能门禁 34181 项断言，零跳过；原生动画契约 30079 项断言，另有 8 个 ASan/UBSan 查询边界用例。
- 全核心 ASan/UBSan 架构门禁 34552 项断言；129 个配置编译单元均检查插桩，没有抑制规则或泄漏诊断。重复实例 RSS 341512192 → 340135936 字节；实际渲染器 llvmpipe (LLVM 22.1.8, 256 bits)。本项不构成目标 GPU 帧率验收。
- 42 项验收工具自检、429 项 C++ /165 项 Python 回归、12 项原生边界、8190 项状态所有权、390 项生命周期、3568 项快照与563 项真实比赛基准检查均通过。
- 修复自检指纹后重新运行当前任务，只需补跑自检，其余有效证据保留；plan-21.1.1 验证通过，git diff --check 通过。未执行 Git 提交或推送。

## 证据与对照保留

- 正式原始数据：.project/optimization/benchmarks/ecs-comparison-1788941698763438575.json；SHA256 f835ae7f77e1859ccfdfad8bedb7fb2040fdfa115e4c4d4d0c30738b7caac2b9。
- 正式日志：ecs_performance-1788940908272071720.log；全核心日志：architecture_regression-1788939799934947345.log；自检补跑：quality_selftest-1788941797094137196.log。
- 首次正式失败 ecs-comparison-1788938218118548256.json 与五对预检 ecs-comparison-1788939575403759109.json 均保留。失败后依据真实调用点优化动画，正式运行前将样本量固定为 15 对/种子；未放宽阈值、提前停止或剔除慢样本。
- 原始 baseline.json 和原始基线二进制不变。额外保存当前已验证的优化二进制，指针 query-optimized.json。
- 优化二进制归档：/tmp/football-optimization-baselines/3bee84158ba91352886b39a342bf814c699a87a950da9314e6bac17ddce092b5。

## 后续边界

本计划完成真实测量与查询/分配优化。内存预算、总体性能预算、手感、网络故障、目标 GPU 渲染性能、AI 对照和最终产品验收仍未完成，继续按 program.json 的依赖顺序推进。行为缺陷与后续参考版本策略见 optimization-known-findings-2026-09-09.md；内存入口及设计见 optimization-memory-review-2026-09-09.md。
