# ms-19.1 框架优化验收记录

验收日期：2026-09-09。此文件记录该里程碑完成时的结果；当前状态由 `python3 .project/quality.py status` 重新计算，后续源码改动会使旧证据失效。

- 建立 8 个里程碑、16 个计划、32 个任务及依赖顺序。
- 新增验收状态机：源码、工具链、环境和日志指纹；超时清理；串行锁；拒绝空测试和跳过；父项与依赖校验。
- 原有完成脚本对 Phase 19+ 使用证据验证，不再修改文本状态或吞掉错误。
- 原生引擎拆为 `libfootball_engine.so`，Python 适配器保留 `libgame.so` 入口。七个原生目标均编译通过，ELF 依赖和未解析符号中不含 Python 绑定依赖。
- Unix 打包包含原生引擎动态库；实际使用 `setup.py` 的选择和复制函数，将产物移到独立目录后成功加载 Python 扩展。
- Debug 编译命令不再包含全局 `-O3` 或 `-DNDEBUG`；检查覆盖 122 个编译单元。

验证：29 项验收流程回归、421 项 C++ 测试、165 项本地 Python 测试全部通过，无跳过。旧网络适配测试不属于本地单元检查，真实网络故障验收单独归入 ms-23.1。

独立进程校验：种子 42/43，每个种子两个进程各推进 1000 帧并恢复初始快照重演。哈希分别为 `25f5d42974a8c79d` 和 `ecbf4729db088aa3`，与拆分前一致。

本机环境：Linux x86_64 / WSL，GCC 16.2.1，CMake 4.4.3，Python 3.14。完整结果及日志在 `.project/optimization/evidence/`。CI 配置已增加原生边界和验收流程检查；本次没有触发远程 CI。
