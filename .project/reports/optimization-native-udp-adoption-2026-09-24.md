# 原生 UDP 接入与验收覆盖修复 — 2026-09-24

2026-09-24：正式完整框架 C 已通过：703 项 C++、783 项 Python 和 305 个子测试；两个种子各 1000 帧、各两个独立进程及快照回放一致。143 项完整 Debug ASan/UBSan 组件测试通过，当前 1117 项源码及产品哈希已核对。真实 UDP 恢复 C 的 Release 两组场景与独立回放通过；Debug、快照中断和真实窗口输入继续串行验收。完整弱网矩阵、默认 UDP 入口迁移、训练和产品级验收仍未完成。

以下保留此前阶段记录：

2026-09-24：训练观测 128 列声明/147 值写入已修复，新增共享布局、编译期维度检查和永久运行合约。Linux Release 与完整 Debug ASan/UBSan 各通过 4 场景、1850 条断言；8 项验收器测试通过。当前源码 1117 项，正式原生/完整框架验收已启动，完整训练与产品级状态仍未通过。详见[实现、证据与边界](../reports/optimization-rl-observation-layout-2026-09-24.md)。

以下保留此前阶段记录：

当前状态：实现已接入，正式 Linux 构建和完整回归待执行，产品级验收未完成。

## 已核对的实现与证据

原生集成 E、恢复 D、快照中断 A 均已有 exit_code=0 终态。恢复与快照中断两组共核对 7007 个权威帧、11231 个保存段帧、12 个快照；最长独立回放约 115 秒，保留原 300 秒限制。证据位于 native-product-udp-recovery-proof-20260924-a。上述记录属于接入前的候选实现。

native-udp-adoption-20260924-a 将共享 TCP/UDP 会话实现、握手、字节流、监听器、连接器及六组永久测试接入正式源码。新增 football_client_udp、football_server_udp 明确入口；旧默认 UDP 入口仍保留。提交 1231ec5 已按用户要求推送 origin/master。此提交不表示当前构建或产品验收通过。

## 验收覆盖缺口与修复

此前 12 个验收项只匹配 engine/tests/fixtures 顶层的 5 个 .inc 文件，漏掉 native_udp 子目录的 12 个夹具。现在使用递归匹配，共覆盖 17 个文件。对照实验在每个验收项上均证明：旧配置未发现嵌套夹具内容变化，新配置使指纹变化；新增嵌套文件也会使指纹变化。

框架回归现在要求至少 702 项 C++ 用例，并分别要求握手 28、窗口 37、字节流 27、监听 20、连接 24、关闭 6 项。测试名必须唯一，失败、错误和跳过均拒绝。702 是保留之前 560 项并加上新增 142 项的验收要求，尚不是当前实际运行结果。

新增 acceptance_inputs_test.py 的 6 项验收器测试在 Windows Python 上通过，覆盖 12 项指纹失效、六组测试遗漏或部分缺失、重复计数、失败/错误/跳过。对照证据还证明旧验收器会接受 702 项完全不含 UDP 测试的报告，新验收器拒绝。这些测试只证明验收逻辑，不替代 Linux 原生运行。

可复现证据：.project/optimization/benchmarks/native-udp-acceptance-inputs-20260924-a，包含原始实现、对照结果、完整日志、源码哈希与退出记录。

## 后续执行

WSL 命令接口的 /bin/true 探针尚无返回，Windows 进程仍存在；无法据此认定 Linux 原生验证已经运行或失败。本轮未启动原生构建，也未重启 WSL、修改全局环境或放宽验收限制。

接口恢复后，先生成包含递归夹具的完整 Linux 源码清单，再串行完成正式 Release、完整 Debug ASan/UBSan 产品与测试构建、六组永久用例、真实 TCP/UDP 对局及恢复/快照中断检查，最后运行完整框架回归。旧的 560 C++ / 783 Python 通过记录是原生接入前的历史证据。

GUI 体验、输入到显示延迟、长期弱网运行、渲染目标硬件、训练可靠性和产品打包等仍需原范围验收，未标记完成。
