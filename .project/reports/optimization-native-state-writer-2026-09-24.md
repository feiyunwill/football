2026-09-24 状态写入终态：engine A 完整Debug引擎构建成功，但测试入口main签名冲突导致阶段失败；新engine B已修正入口与保存模式eos，11项追帧测试、真实写入契约、双向各1500帧跨版本快照及两个种子的快照契约均通过。B实际TCP窗口完成启动与暂停/恢复，随后客户端因 Local input frame is outside the retained window 退出：1496客户端步骤对2542服务端步骤，仅12次呈现，原1000门槛未通过。3799输入/34日志已核验。当前 native-state-cache-purity-engine-20260924-a 正在验证保存状态不应使场景缓存失效或请求球网上传的修正；尚无其运行验收结论。正式源码未采纳。以下为历史记录。

# 原生客户端追帧与状态序列化优化

归属 ms-23.1 → plan-23.1.1 → task-23.1.1.2，关联架构、性能、网络和渲染。当前为私有候选，未正式采纳，产品验收未完成。

完整 Debug 在原30秒内没有开始比赛呈现。客户端每帧计算状态摘要，同时每批追帧后还可能为已经收到权威输入的下一帧建立预测。现有窗口循环要求权威队列清空后才绘制；当处理速度不足时，该条件会持续阻止比赛呈现。两次只添加计时的诊断复现同一失败，保留了原启动和退出门槛。

第一轮诊断中，客户端292次逻辑步骤覆盖12.735秒；摘要291次、中位12.132ms，总计3.473秒，另有98次预测快照。服务端531次步骤覆盖13.137秒，摘要仅53次。第二轮细分显示球员状态占序列化的大头，字符串和动画引用占比较小。分段计时有嵌套及观察开销，不能相加或当作无观察器的正式性能结果。两轮各2748项固定输入及3份命令日志哈希通过复核。

独立标量写入对比每轮序列化20次、每次32768字段，报告9轮中位。完整Debug原始202.473ms，直接append为158.997ms，分块初始化缓冲为71.642ms；Release原始5.088ms，分块3.344ms。全部策略通过已知字节、回读、跨块续写、canonical排除、诊断比对与错误输入检查。分块策略保持字节顺序及大小限制，在GetState返回时裁去未使用空间。这些仅证明组件行为与该测量下的收益。

追帧候选继续调用输入发布器，但在连续权威输入未消化或下一帧权威输入已存在时不创建重复预测。11项测试通过，包括原6项模拟测试和5项新场景；旧实现的新场景测试按预期失败3项。1000帧批量追赶仍逐帧核对输入与状态哈希。

当前 native-state-writer-engine-20260924-a 正在完整Debug串行构建，随后运行实际引擎快照契约、原真实TCP/UDP窗口与基准引擎独立回放，成功后再运行Release。原30秒启动、250ms整个进程退出、1000次呈现、300秒独立回放和完整sanitizer参数保持不变。

审查发现还需补全保存模式eos()对缓冲余量的处理。修正和1500帧跨版本快照工具已准备但尚未执行；因此A即使通过也不直接采纳。后续必须使用新阶段，不修改已执行输入。Host产品菜单、完整恢复与弱网矩阵、原始UDP中立帧和RSS失败等原有缺口仍保留。
