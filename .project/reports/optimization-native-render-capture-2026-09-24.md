2026-09-24 姿态捕获整机终态及状态哈希对照：capture engine A已exit1，8488输入/50日志核验；组件/双向1500快照/946与950恢复通过，实际TCP仍输入窗口耗尽（9380客户端步/10457服务端步、20呈现）。原程序不变的增量日志窗口验证正在运行，11边界测试及270600条旧记录计数等价已通过。独立SHA-256对照在完整Debug调用端与Release各通过2048断言：87132字节、每批100次、9批中位，原/现有OpenSSL实现Debug为277.404/2.587ms，Release为25.409/2.343ms，完整摘要与协议前8字节一致；358输入/4日志核验。现仅准备provider候选，尚未整机采纳或验收。以下为历史记录。

# 物理插值姿态保存纯读取

归属 ms-23.1 → plan-23.1.1 → task-23.1.1.2，涉及架构、性能和显示连续性。候选未采纳，产品未验收。

上一动画/批量写入整机候选通过完整Debug组件、双向1500帧快照和两个种子的恢复，但真实TCP在19次呈现后输入窗口耗尽，客户端8651步/服务端9707步。客户端保存物理姿态中位1.250ms，逻辑步15.746ms，摘要4.083ms；这些是嵌套并含观察开销的计时，不能相加或当作正式性能结论。7448输入、48命令日志核验，无变化。

源码确认SaveInterpolationState(false)原来调用UpdateFullbodyNodes，既更新全身和头发显示观察者，也覆盖供from_display保存使用的显示关节。新实现直接把逻辑骨架的派生位置/旋转复制到previousRenderJoints，保留原坐标镜像数学，避免产生显示更新。from_display=true继续保存当前显示端点。

真实球员组件已复现旧显示通知反例。旧参考248项、新候选306项断言通过，两种镜像下的身体附件、插值端点与中间位置、显示修正姿态摘要均为2054272833542212839。新增检查确认物理捕获不覆盖上次显示附件姿态。此证据不包含真实GPU窗口吞吐或整机性能收益。

native-render-capture-engine-20260924-a继续完整Debug与Release的原组件/快照/恢复/窗口/独立回放门槛，并增加显示准备、模型变形和图形提交计时。当前正在执行，未取得整机终态。所有已执行阶段保持不可变。

永久候选已含41文件与新的306断言姿态契约；双构建状态检查共22个结果（18组件/种子用例加4跨构建快照操作）。隔离准备树1934文件、1161状态源文件，未排队、未执行，ready=false。控制门禁、完整弱网恢复、Host菜单、原中立输入/RSS原因、性能基线、硬件与训练打包等产品范围仍未全部完成。

2026-09-24 当前阶段进展：native-render-capture-engine-20260924-a的完整Debug组件与双向各1500帧旧/新快照通过，两侧各2999哈希核对/1499续跑，正在运行946/950项恢复检查。窗口与Release仍待验证。另发现旧窗口工具每次轮询都重新解析整份events.jsonl，并准备native-trace-reader-preparation-20260924-a：增量读取、11项边界测试、旧保留日志逐条计数对照、相同产品二进制/核心/原C++观察器的窗口复验。全部尚未执行或排队，须等当前阶段真实终态；不得把观测开销假说当作已证实的窗口失败原因。原300秒/1000帧及250ms退出等门槛不变。
