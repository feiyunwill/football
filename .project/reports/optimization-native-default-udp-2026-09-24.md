# 默认原生 UDP 入口迁移候选与验收 — 2026-09-24

2026-09-24 提交前核验：正式框架与架构检查均已通过；多席位运行 C 的 Release/完整 Debug 四场合计 4007 个权威帧、6 个恢复快照、4859 个保存段回放帧，全部 AI 指令与独立重建一致。默认 UDP 候选构建及 28 项非法参数拒绝检查通过，四场实际运行合计 4004 个权威帧、6 个恢复快照、4845 个保存段回放帧。新增永久测试候选在 Release/完整 Debug 各通过 56 项 C++ 测试，验收器自测 21 项通过。本次复核终态、执行日志哈希和两份 56 项 CTest XML；候选尚未接入正式源码，菜单、完整弱网矩阵、资源容量、延迟及整体产品验收仍未完成。以下排队/运行中描述为历史记录。

对应 ms-23.1 → plan-23.1.1 → task-23.1.1.2。当前为已准备并排队的私有候选，尚未验证通过或接入正式源码。

## 当前实现与迁移要求

正式 CMake 的 football_server 指向 asio_server_engine.cpp，football_client 指向 integrated_client_udp.cpp；明确命名的 _tcp/_udp 入口已使用共享的恢复会话实现。旧公开服务器默认 slots_per_client=0，代表单客户端取得全部可用席位；共享明确入口默认为 1。迁移必须保留这个差异及端口、阵容、种子和可选席位参数。

候选 native-default-udp-migration-20260924-a 将公开服务器/客户端指向共享 integrated_server.cpp/integrated_client.cpp 并启用 UDP 定义，链接恢复库；只有公开服务器增加默认全部席位定义。数字解析使用严格 from_chars，保留旧入口对符号、空白、越界和错误参数数量的拒绝。单独配置实际 CMake 源码镜像，编译公开目标并复用当前指纹已固定的引擎/恢复库；核对真实编译依赖及链接闭包，不能将此称为重新构建整个核心。

## 自动验证顺序

前置条件是 native-slot-group-runtime-candidate-20260924-c 的 4 个完整实际场景与独立回放全部通过。之后串行执行：

1. Release 和完整 Debug ASan/UBSan 的公开入口构建，核验实际来源、宏和恢复库链接。
2. 每种配置 14 项非法命令行参数拒绝，共 28 项，要求初始化之前失败且无 sanitizer 诊断。
3. native-default-udp-runtime-20260924-a 的两种配置、两类 22 席实际窗口场景。单客户端场景刻意省略第六个席位参数，验证公开默认行为；双客户端场景显式指定每客户端 11 席。
4. 保留每场至少 1000 帧、每阶段整组连续 8 帧真实按键、断线恢复确认首发丢失、权威记录与网络抓包独立比对、恢复快照和 300 秒独立回放预算。AI 指令逐帧与独立 GameEnv 重建结果相符，并保留正向动作及合法等待判据。

所有队列输入不可修改；正式 1120 项源码继续冻结。

## 不能扩大的结论

Python main_menu.menu_host_game/menu_join_game 直接使用 HostedMatch/NetworkPlayer，图形分支由 _run_graphical 承接。更换 C++ 公开二进制并不能证明菜单已改用原生会话，也不能替代暂停/保存/场景选项兼容性验证。

菜单接入、旧协议负面场景、资源故障/容量、完整弱网矩阵、输入延迟和产品级状态仍未完成。候选通过后仍需正式接入、永久回归及原范围正式验收。
