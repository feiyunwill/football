# 加载取消传输契约与架构门禁候选修复

2026-09-24 提交时状态：正式 loading_regression.py 已接入 TCP/UDP 的 transport 字段校验及两种传输各 5 个取消场景，保留 250 毫秒上限并拒绝负值。新增 loading_acceptance_test.py 的 13 项永久回归通过；native_boundary 通过。正式框架检查为失败（711 项 C++ 通过；Python 780 项及 305 子测试通过，3 项图形测试因未提供 DISPLAY、无法获得 SDL 窗口而失败）；正式架构检查仍在运行，不能标为整体通过。以下较早记录保留为历史。

2026-09-24 候选终态已通过：loading-transport-gate-candidate-20260924-a 于 1790197680.5891025 结束，退出码 0。14 项验收器反例、完整 Debug 架构 1,632,099 条断言、Release 加载 32 个结果全部通过。Debug 加载为 2190 条断言、Release 为 2175 条断言，均保留 26 个取消检查点、19 个准备操作、12 个所有权场景、11 个加载协议场景、2 个终端流场景及 TCP/UDP 共 10 个实际取消场景。

已复核候选输入哈希、三条成功执行日志及全部 172 个 Debug 编译单元：保留 -g、ASan/UBSan、禁止恢复、帧指针和泄漏检测，无 O1/O2/O3。此结果仍是私有候选验证；正式 loading_regression.py 尚未接入，原 architecture_regression 失败记录继续有效。必须等当前多席位串行链结束后才能修改正式源码并执行正式验收。


2026-09-24 运行观察：候选完整 Debug 的种子 42/43 正常、反向及渲染快照契约均已通过，当前继续重复实例与资源生命周期检查。此处记录过程证据，不作为候选整体或正式架构通过结论。

2026-09-24 后续进展：私有验收器 14 项反例检查通过；完整 Debug 运行已通过 TCP 和 UDP 各 5 个真实取消场景，最大退出时间分别约 0.101 秒和 0.101 秒，整个架构检查仍在运行。尚未接入正式门禁。

本轮正式输入、战术集成、AI 决策均已通过，断言数分别为 3,065,419、146,349、22,950。跨域整体验收因原架构门禁失败而为失败；这些通过结果不能替代完整训练、硬件性能或输入延迟验收。


对应 ms-20.1 → plan-20.1.2 → task-20.1.2.2；当前架构门禁为失败，候选尚未执行。

## 已证实的失败

正式 architecture_regression 在 2026-09-24 的执行以退出码 1 结束。实际客户端加载取消契约完成 5 场景、35 条断言，输出 transport: TCP，最大退出时间约 0.1006 秒；loading_regression.probe 仍要求已被共享 TCP/UDP 输出替换的 real_tcp: true，因此报 Loading contract coverage changed。原日志和失败 JSON 已保留并随 ea92986 提交。

图谱与实际源码核对了 loading_regression.probe 的两个直接调用方：独立加载检查及 architecture_regression.main。客户端契约中的预处理输出段索引覆盖不完整，已读取实际源码确认 TCP/UDP 分支。未将测试输出通过误记为完整架构通过。

## 私有候选与验收

loading-transport-gate-candidate-20260924-a 保存原门禁和原失败日志。候选将 TCP 期望字段改为 transport: TCP，并新增真实 UDP 取消契约，分别保留 5 场景、35 条断言和小于 250 毫秒的限制，同时拒绝负时间。

原 26 个取消检查点、19 个准备操作、12 个所有权场景、2 个终端流场景及 11 个加载协议场景全部保留。候选完整加载检查包含 32 个结果、10 个实际客户端取消场景。

14 项验收器测试以原真实输出作为固定输入，验证旧门禁确实拒绝当前 TCP 输出，并拒绝传输混淆、缺失字段、覆盖缩减、跳过、失败、非法时间、重复结果和 sanitizer 诊断。该单元测试中的 UDP 行明确为合成反例数据；实际 UDP 验收必须由后续真实契约提供。

候选排在多席位私有集成终止之后，依次运行验收器测试、完整 Debug 架构检查和 Release 加载检查。正式源文件未修改，必须在候选成功后另行接入及执行正式验收，不能以私有通过替代正式状态。

## 其他域的当前证据

同一正式源码上的 input_contract 已通过：3,065,419 条断言，包含真实 GameEnv、X11/XTest、原生入口及原有 TSan 专用工具。日志与报告哈希已核对。报告明确 latency_acceptance=false，不构成端到端输入延迟通过。战术与 AI 仍须等待本轮正式终态。
