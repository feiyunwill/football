# ms-20.1 架构里程碑验收

2026-09-09，`python3 .project/quality.py run task-20.1.2.2` 退出成功，`verify ms-20.1` 返回 `Verified: ms-20.1`。下一任务为 `task-21.1.1.1` 真实比赛基准。整体产品优化目标继续执行，未提交或发布。

| 任务 | 实现及验收 |
| --- | --- |
| 20.1.1.1 状态所有权 | OOP 权威状态、ECS 派生查询缓存，初始化/帧结束/快照恢复统一刷新；8,190 条真实引擎断言 |
| 20.1.1.2 生命周期 | 实例所有权、嵌套 TLS、串行 API、幂等 close/重开、资源释放、并发环境、字体和图形失败恢复；本轮 390 条断言，含真实 Python 绑定的 36 条检查 |
| 20.1.2.1 逻辑与表现 | 版本化快照、有界解析、异常时原帧恢复、规范 RNG 状态、真实渲染不改变逻辑；3,568 条断言 |
| 20.1.2.2 架构集成 | 完整原生核心及测试宿主启用 ASan/UBSan，真实生命周期、半场、输入、快照和图形资源矩阵；4,473 条断言 |

本轮修复的实际问题：

- 默认半场哨兵值与 1800 相乘发生有符号整数溢出，被完整 UBSan 首次暴露；改为 64 位计算，并测试真实半场切换和默认值。
- 权威输入解码没有启用重置后由内置 AI 接管的控制器；现在与本地 action 使用相同启用逻辑，通过两侧各两个控制槽、40 帧的逐帧状态对照。
- 非法控制槽、动作、场景配置及输入帧存在越界或部分修改风险；现在在修改前验证。输入帧后续槽无效时，也不会修改前面的控制器或推进比赛。
- 原生单机缺少可用的暂停/恢复/结束流程；新增 GameEnv 接口及 Python 导出，P/Esc 暂停或恢复，Q/关闭窗口结束，失焦暂停。暂停快照保留冻结状态，恢复后的未来轨迹保持一致，结束后仍可读取最终观察和渲染，并可 reset。
- SDL/EGL 部分启动失败会退出宿主，字体描边对象失败未校验；现抛异常，清理后可重试。
- 奇数宽 RGB 截图可能因 GL 行对齐写出缓冲区；显式设置紧密行布局并恢复原 GL 状态，验证真实 321×181 截图。

正式矩阵覆盖两种子的完整生命周期及局部/权威输入等价性、EGL 生命周期、双向损坏快照、有图形/无头轨迹比较、重复与并发实例、双 EGL 上下文、SDL 事件队列、SDL 无 GL 能力、损坏与缺失字体。全部无跳过；ASan/UBSan 遇错即失败，启用泄漏检测，无 suppressions。126 条编译命令均核验插桩标志，实际构建并运行原生核心和对应测试宿主。

重复生命周期测试热身 RSS 为 340,553,728 字节，末轮为 340,688,896 字节，差值 135,168 字节；ASan quarantine 明确为 16 MiB。本数字属于插桩运行，不用作 Release 性能基线。

前置框架检查重新通过：29 项验收工具测试；7 个原生程序与 Python 适配器构建、ELF 依赖和移动打包加载；421 项 C++、165 项 Python；Debug 编译参数；两种子各两独立进程 1000 帧及快照重演。当前 hash 仍为 seed42 `606593fad9c78ea9`、seed43 `a8c11d6b25d70198`。`git diff --check` 通过。

正式日志位于 `.project/optimization/evidence/`，对应 JSON 保存源码/工具/环境指纹和日志哈希：

- `quality_selftest-1788931137806947204.log`
- `native_boundary-1788931139174395990.log`
- `framework_regression-1788931393054224901.log`
- `state_ownership-1788931422804346363.log`
- `environment_lifetime-1788931427245489659.log`
- `simulation_contract-1788931452044608338.log`
- `architecture_regression-1788931471405389903.log`（734.997 秒，4,473 条断言）

架构检查源码指纹：`dd685e6440d90a872b0d04109d4e77157a37ddf4467c3cc3fa7a6094f19f161d`。

验收范围：Linux 本机完整原生核心；Python 适配器另以真实扩展验证。实际 GL renderer 为 llvmpipe，SDL 使用 offscreen 驱动的真实窗口/context 和事件队列，不代表目标桌面或 GPU 帧率。旧无版本快照被拒绝，v2 载荷面向当前 Linux little-endian ABI。CI 已接入相同检查，但未远程执行。性能、手感、完整网络故障矩阵、渲染效果与 GPU 性能、AI 和发布条件继续由后续里程碑验收。
