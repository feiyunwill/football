## 2026-09-24 提交前验证状态

正式 RTT 修复的网络验证链已通过：8 组真实客户端/服务端场景、9010 个权威帧、16 次快照、16 个真实窗口输入动作；框架验收为 711 个 C++ 用例、783 个 Python 用例。可提交的结果摘要和源文件哈希保存在 .project/optimization/evidence/native-input-rtt-chain-20260924/。构建产物哈希代表该验证链结束时的状态；后续跨域检查会重新配置临时构建目录。

跨域架构回归尚未通过：loading_regression.py 要求客户端 drain 结果包含 real_tcp: true，实际契约输出为 transport: "TCP"，导致 Loading contract coverage changed。该失败已保留，未修改门禁或将整体质量标记为通过。后续输入、战术、AI 检查及多席位候选仍在运行或排队。

# 原生 UDP：恢复验收与真实窗口输入缺陷

2026-09-24：RTT 输入调度接入后的正式门禁 D 已通过：711 项 C++、783 项 Python、305 个子测试，151 项完整 Debug ASan/UBSan 组件测试；172 个 Debug 编译单元保留调试信息，两个种子各 1000 帧、各两个独立进程和快照回放一致。1120 项源码、十份产品产物与执行日志哈希已核对。真实 UDP 恢复 D 已开始，快照 D、图形输入 C、完整证据核验 B 继续串行执行；之后串行重验原有架构、输入、战术及 AI 门禁。默认 UDP 入口与单客户端多席位迁移尚未完成，完整弱网、端到端延迟及产品级状态仍未通过。

以下保留此前阶段记录：

2026-09-24：最终 RTT/连续发布候选 D 已完成 Release 1000 帧、完整 Debug ASan/UBSan 1001 帧真实窗口输入及独立回放；每组八段连续八帧按键、两次恢复、两个快照通过。采用前核对了 1513 项输入哈希、两份客户端产物及 16 条执行日志。现已将三份实现按候选原样接入正式源码，增加八项永久回归测试及两份旧算法对照夹具。夹具采用已有递归指纹覆盖的 .inc 格式，正式源码共 1120 项。八项验收器回归通过，完整框架门槛改为 711 项 C++，Debug Sanitizer 组件门槛改为 151 项。新正式门禁 D → 恢复 D → 快照 D → 图形输入 C → 证据核验 B 已按依赖串行启动，尚未取得完整正式通过结果。默认 UDP 入口、完整弱网矩阵、端到端延迟和产品级状态仍未通过。

以下保留此前阶段记录：

2026-09-24：正式恢复 C 的四组 Release/Debug 场景及快照中断 C 的两组场景全部通过。六组独立 GameEnv 回放共验证 7005 个权威帧、12 个恢复快照；输入窗口和原超时预算未放宽。正式源码仍为 1117 项。

真实窗口输入 B 暴露 Release 缺陷：窗口采样和客户端发送正确，但客户端 0 的 706 个非零输入只有 16 个进入相同权威帧。原调度仅以最新已收权威帧加一作为提前量，故障场景 RTT 约 100 ms，大量输入在服务端采集截止前仅约 13 ms 才从客户端发出。原 Debug 场景及其独立回放通过，不能代替 Release 失败。

候选 B 使用 RTT 与波动预算，将提前量限定在原 16 帧服务端窗口内。初始按键改善，但满窗口后时钟把等待计作跳帧，恢复后产生帧缺口，Release 场景仍失败；Debug 1002 帧回放通过。

候选 C 修正稳定提前量下的连续发布，以及窗口容量恢复时的时钟处理。Release 已完成八段各连续八帧按键、两次断线恢复、1002 帧和两个快照的独立回放，Debug 仍在运行。候选 D 又补上长时间停顿后权威帧越过已发布输入时重新设置提前量的边界测试，已串行排队。两者尚未接入正式源码。

所有失败目录保留。代码与产品验证位于 native-input-rtt-candidate-20260924-a/b/c/d，基准失败位于 native-canonical-udp-gui-input-20260924-b，抓包分析位于 native-gui-input-failure-analysis-20260924-a。完整弱网矩阵、默认 UDP 入口、端到端延迟与产品级验收仍未完成。
