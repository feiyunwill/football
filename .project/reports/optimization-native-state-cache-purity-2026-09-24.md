2026-09-24 缓存候选终态与镜像诊断：cache-purity B已exit1，节点/球网22断言、双向各1500帧快照以及946/950断言恢复契约通过，但实际TCP仅11次呈现，客户端仍报输入帧超出保留窗口（1575客户端步骤/2617服务端步骤）；4840输入、36日志核验。仅增加管线分段计时的新诊断也复现失败，4848输入、3日志核验，耗时差异集中在含坐标镜像的步骤，球员处理本身接近。新native-mirror-graphics-engine-20260924-a只在人物临时镜像时排除图形观察者通知，保留派生缓存失效和其他系统通知，并验证最终Put姿态；当前串行验证中，未采纳。以下为历史记录。

2026-09-24 局部验证：cache-purity B的完整Debug已复现节点/球网两个旧版反例，新版节点13断言、实际球网9断言全部通过，写入与11项追帧回归也通过。正在复验跨版本快照；实际窗口、Release及整个产品仍未验收。

2026-09-24 缓存验证工具修正：cache-purity A已完成完整Debug核心库链接，但契约工具使用了不存在的四元数相等运算和私有球网更新方法而编译失败；4312输入/16日志核验。新cache-purity B以相同产品源码/核心库继续，测试改为逐分量比较和公开Match::Put路径；当前仍未取得缓存与窗口验收终态。以下为历史记录。

# 状态保存的场景缓存副作用

归属 ms-23.1 → plan-23.1.1 → task-23.1.1.2，关联架构、性能和渲染。当前为私有候选验证，未采纳。

分块状态写入与追帧优化的完整Debug实际引擎测试通过双向各1500帧快照兼容、2999次消费哈希检查和1499次连续推进；两个种子的快照契约亦通过。但实际TCP长程中客户端只完成12次呈现，逻辑1496步落后服务端2542步，最终输入帧超出保留窗口而退出。3799固定输入和34命令日志已核验，原失败保留。

审查发现Node::ProcessState在保存时也使所有节点派生变换和包围盒缓存失效，Match::ProcessState也设置球网重置/上传标志。候选把这类缓存更新限制在读取状态时，保持快照字段、字节顺序和校验格式不变。保存与摘要应读取场景，不应额外安排渲染缓存重建。

native-state-cache-purity-engine-20260924-a正串行验证两个真实旧版反例、新版节点层级与实际球网Geometry更新契约、跨版本快照、真实恢复以及原TCP/UDP窗口门槛。节点测试还检查既有dirty状态不被保存清除，恢复父子节点后派生值正确；球网测试检查恢复仍安排上传，摘要不吞掉已待上传的变化。完整Debug使用相同头文件和其余对象，仅重编译Node与Match并替换对应静态库成员；Release计划完整构建。

当前尚无本阶段运行终态，不能认定该修正解决了所有客户端落后或图形验收问题。全局目标、Host产品菜单、网络恢复/弱网矩阵、原UDP中立帧、RSS、性能基线、目标硬件渲染和训练打包等未完成项保留。
