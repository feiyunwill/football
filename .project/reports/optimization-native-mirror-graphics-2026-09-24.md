2026-09-24 镜像D终态与下一优化：D完整Debug双向1500帧快照、两个种子恢复及19项镜像断言通过，但实际TCP在16次呈现后仍输入窗口耗尽；客户端4228步/服务端5270步，步耗时中位16.706/14.224ms，客户端摘要仍6.261ms。6900输入/37日志核验。独立批量写入对照随后通过：旧/新各24832断言、360448字节完全一致，组件20次序列化中位345.889→104.618ms，6911输入/6日志核验。新native-animation-state-engine-20260924-a合入普通保存批量写入和逻辑动画图形通知排除，正在完整Debug串行验证；正式源未采纳，产品验收未通过。以下为历史记录。

2026-09-24 局部验证通过：mirror D已复现旧通知反例；旧核心参考与新候选各通过19断言，碰撞/最终显示姿态摘要均为10057015697396660735。初始层级缓存修正后夹具通过。接下来仍须跨版本快照、真实TCP/UDP窗口、独立回放及Release；尚不代表吞吐或产品验收通过。

2026-09-24 镜像夹具初态核实：mirror B/C均在旧核心参考模式的往返断言中exit1，各5861/6371输入、各9日志核验。C详细值显示初始派生位置遗漏根节点平移（0.35,0.7,1.05→-24.65,0.7,1.05），旋转前后均为单位四元数；因此不是新候选独有差异，也不能归因为四元数符号。新mirror D先显式完成初始层级缓存更新，再验证往返、派生坐标与旧/新位级姿态摘要，产品源码/核心库保持A相同。当前运行中，未采纳。以下为历史记录。

2026-09-24 镜像契约修正：mirror A完整Debug核心库构建及旧图形通知反例已通过，随后测试的“世界坐标XY直接取反”断言失败；5351输入/12日志已核验。新mirror B保持相同产品源码和核心库，改用局部层级变换合成派生坐标期望，并单独运行旧核心参考模式、逐位比较旧/新碰撞和最终显示姿态摘要。当前验证中，不把断言失败直接归为产品修正成功。以下为历史记录。

# 临时坐标镜像的图形观察者开销

归属 ms-23.1 → plan-23.1.1 → task-23.1.1.2，关联架构、性能和渲染。当前为未采纳候选，尚未取得运行验收结论。

cache-purity B通过真实缓存反例/正例、双向各1500帧快照与两个种子恢复契约，但完整Debug实际TCP窗口仍在11次呈现后失败，客户端1575步/服务端2617步，客户端输入帧超出保留窗口；4840固定输入和36命令日志核验。不能把缓存修正描述成吞吐问题已经解决。

独立诊断只替换计时观察库，复用完全相同的产品程序和核心库，再次失败；4848输入和3日志核验。球员处理时间接近，而含多次Match::Mirror的队伍处理、控球统计等步骤明显更慢。计时有观察开销且阶段嵌套，不能相加或称正式性能达标。

代码路径是Match::Mirror → Team/PlayerBase::Mirror → HumanoidBase::Mirror → Node::RecursiveUpdateSpatialData → Geometry::RecursiveUpdateSpatialData。后者在图形模式下逐个通知观察者，并立即计算派生位置/旋转；无图形服务端不会执行这些通知。图谱Verify发现不完整的动态调用边，相关精确源码已读取；match.cpp1397部分索引已用1394–1401源码补查。

候选将人物镜像的旋转赋值改为不隐式递归，随后显式递归更新并排除Graphics系统。碰撞和关节需要的派生缓存仍全部失效，非图形观察者仍收到通知。实际显示使用独立fullbodyNode和头发对象，由UpdateFullbodyNodes/Put更新。候选不改变临时镜像的数学运算、逻辑状态、快照格式或比赛规模。

新阶段native-mirror-graphics-engine-20260924-a将验证旧实现确实发布临时图形变换，新实现仍更新非图形观察者、单次镜像碰撞坐标、双次镜像还原与最终正常/镜像Put显示。随后执行原跨版本快照、恢复、真实TCP/UDP窗口及独立旧核心回放，完整Debug通过后再完整Release。没有放宽30秒启动、1000呈现、250ms整个进程退出或sanitizer要求。

永久控制门禁已准备30文件候选，入口覆盖双构建12普通会话客户端、6过期快照恢复客户端、4实际窗口和22次独立回放；当前只完成语法解析，注册项ready=false，未采纳或执行。Host产品菜单、完整恢复/弱网矩阵、原中立帧/RSS因果、性能基线、目标硬件及训练打包等仍未完成。
