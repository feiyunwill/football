# 原生 UDP 产品集成、实际恢复与弱网验证

2026-09-24：候选原生集成、恢复和快照中断验证已通过，共享 TCP/UDP 实现已接入并提交；当前正式 Linux 构建与完整回归仍待执行。已补齐 12 个验收项对嵌套夹具的指纹覆盖，并要求六组原生 UDP 用例完整出现；6 项验收器测试通过。此前完整框架通过记录属于接入前源码，产品级状态仍未通过。详见[当前实现与验收边界](../reports/optimization-native-udp-adoption-2026-09-24.md)。

以下保留此前阶段记录：

2026-09-22 最新终态：当前完整框架门禁因 Python 最大快照 UDP 恢复失败而结束，后续集成/恢复未启动。文件读取原生回归仍通过，整体验收未通过；网络调度候选尚未采用。见[失败与复现](optimization-python-udp-snapshot-2026-09-22.md)。

以下保留此前阶段记录。

2026-09-22 最新：文件读取优化已接入正式源码（1090 项），完整框架复验正在执行；本报告先前核心与结果保留为历史，当前启动及恢复验收仍待复验。见[最新记录](optimization-native-file-read-2026-09-22.md)。

以下保留此前阶段记录。

2026-09-22 回放诊断终态：恢复矩阵 B 以失败结束，保留 Debug 30 秒启动超时和独立回放 300 秒超时。新增阶段进度诊断完整验证了该 Debug 对局的 1002 帧权威轨迹、1617 帧保存段与两个快照，11940 条断言通过；诊断约 315 秒，不替代原时限验收。启动剖析完成，动画读取优化候选已进入双构建验证，尚未采用。见[本轮定位与优化](../reports/optimization-native-animation-load-2026-09-22.md)。

以下保留此前阶段记录。

对应里程碑 **23.1 → 计划 23.1.1 → 任务 23.1.1.2**。UDP 合并候选 C 整批通过；实际恢复矩阵 B 的 Release 部分通过，Debug 已有启动超时失败，整体不通过。正式源码仍是 ASE 修复接入后的 1089 项，UDP 候选尚未采用，网络与产品整体验收不提升。

## 合并候选 C 终态

| 原有检查 | 实际结果 |
| --- | --- |
| 真实产品入口 | TCP/UDP 客户端与服务器 × Release/Debug，共 8 个 |
| 加载取消 | 20 项、140 条断言；最长 107.651ms，原 250ms 不变 |
| UDP 末尾报文和停服回收 | 每种构建 6 项，共 12 项，无跳过 |
| 真实双客户端对局 | 两种传输 × 两种构建，共 4 组，客户端与服务器均正常退出 |
| 独立引擎回放 | 8 份客户端日志共 1608 帧、5680 条断言全部一致 |
| Debug 服务器启动 | TCP 13.495 秒、UDP 21.317 秒，原 30 秒不变 |

Release 每个客户端保存 200 帧；Debug 每个客户端保存 202 帧。回放核对实际完整日志及双方同帧哈希。末尾报文用例采用真实 UDP、公共服务器和显式 oracle 引擎；真实对局、加载取消和独立回放采用实际 GameEnv。Debug 保留 -g、完整 ASan/UBSan 和泄漏检测。

C 退出码 0，14 个结果条目全部通过；16 个测试及产品二进制摘要、终态、XML、回放摘要和 1089 项正式源码一致性已复核。见[C 终态报告](../optimization/benchmarks/native-product-udp-integration-20260921-c/report.json)及[逐项复核摘要](../optimization/benchmarks/native-product-udp-validation-evidence-20260922-a/validated-summary.json)。

## 实际恢复矩阵 B：Release 已通过

两种场景均使用 C 的真实 UDP 客户端、服务器和实际核心库，逐进程核对核心映射。固定 seed 42、1v2、两名真实客户端，每人完成至少 1000 帧。每名玩家分别断网 3.4 秒，并丢失首次恢复 Accepted 数据报，由真实传输层重传恢复。

| 检查 | 仅断网 | 叠加固定弱网 |
| --- | --- | --- |
| 两端最终权威游标 | 1000 / 1001 | 1002 / 1000 |
| 自动恢复 | 两端各恢复一次，保留原比赛和席位 | 两端各恢复一次，保留原比赛和席位 |
| 安装并保存的快照 | 每端 1 个 | 每端 1 个 |
| AI 接管期间有效输入帧 | 156 / 172 | 138 / 148 |
| 独立权威轨迹 | 1001 帧 | 1002 帧 |
| 客户端保存段独立回放 | 1676 帧 | 1682 帧 |
| 客户端/服务器退出 | 全部 0 | 全部 0 |

共 2003 帧权威轨迹、3358 帧客户端保存段和 4 个检查点，在独立 GameEnv 中全部复验一致。每个客户端的检查点边界、逐帧输入、状态哈希、最终游标与最终哈希都与独立权威轨迹一致。另一名玩家没有意外重连；AI 接管、交还通知及恢复后的中性玩家输入均检查通过，心跳回执也有真实记录。

固定弱网在每端施加 10–55ms 延迟；两端合计主动丢失 104 个数据报和 92 个 ACK，安排 94 次复制，实际记录 1078 次不同序号的乱序交付。另有两次断网期间的丢包和两份首次恢复确认丢失，单独计数。该矩阵证明这组确定性故障下的恢复行为，不等同于所有网络条件。

中继只延迟、丢弃、重复或原样转发完整数据报，不裁剪应用帧、不伪造 ACK。原始报文、权威输入、事件、真实客户端回放及独立复验日志均保留。见[B 当前结果](../optimization/benchmarks/native-product-udp-recovery-20260922-b/results.json)、[固定的本轮观察](../optimization/benchmarks/native-product-udp-validation-evidence-20260922-a/recovery-b-observation.json)和[证据文件摘要](../optimization/benchmarks/native-product-udp-validation-evidence-20260922-a/proof-files.json)。

## Debug 当前失败与下一步

B 的无额外弱网场景在原 30 秒服务端绑定期限内未完成启动，客户端尚未启动；测试清理发送 SIGTERM 后服务器退出 -15。没有将该项跳过或替换为后一次通过。见[原始失败报告](../optimization/benchmarks/native-product-udp-recovery-20260922-b/sanitized-outage/report.json)。

另一组 Debug 固定弱网实际对局通过，启动 14.789 秒，两端最终游标为 1002 / 1000，均自动恢复一次、保存一个检查点并正常退出；这一组的独立回放尚待终态。C 先前两组启动通过、B 一组失败、另一组通过，说明当前单次通过不足以证明启动可靠性。

已排队 [当前源码剖析 B](../optimization/benchmarks/native-startup-cost-profile-20260922-b/plan.json)：等待恢复 B 终止后，使用当前 1089 项源码构建的核心，先运行未插桩对照，再对两个种子分别进行三次 ASE 解析/构建/释放、角色构造和加载检查点测量。Release / 原 Debug 串行，共 4 次对照及 12 次剖析；同时记录实际耗时与线程 CPU，核对初始状态哈希。

剖析的 90 秒观察上限只用于查明耗时，不替代产品 30 秒启动门禁；启动失败、Debug 完整 sanitizer 和泄漏检查均保留。插桩复用已审查的私有实现，并校验相应正式源文件摘要未变，链接当前含 ASE 修复的核心对象。此次只做诊断，不采用未经证明的身体顶点索引候选。

最新 [恢复状态快照](../optimization/benchmarks/native-udp-recovery-diagnostic-evidence-20260922-a/recovery-b-observation.json)及[剖析进程快照](../optimization/benchmarks/native-udp-recovery-diagnostic-evidence-20260922-a/startup-profile-b-observation.json)分别记录观察时间；终态必须以各自 exit/report 为准，最后一次 status.phase 不能代表进程仍活跃。

## 测试边界与尚未完成项

C 的 Debug 客户端指定 --frames 200，实际在完整权威批次处理后保存 202 帧。初版验证 A 原先假设精确帧数，因此在依赖等待期明确终止，没有执行产品场景，原文件与终态完整保留。

B 保留至少 1000 帧要求，完整捕获两端实际保存的最长权威前缀，并独立检查各自最终游标，额外尾部最多 1024 帧。没有修改产品行为或裁剪 UDP 数据；这属于测试边界修正。

B 的启动超时须定位修复，Debug 剩余回放须取得终态；随后覆盖加载期断线、快照中途断线、旧代际迟到数据与 ACK、长时间弱网及实际窗口。Q 到整个进程退出 250ms、50ms p95、目标硬件及原有 Python UDP 缺口仍须独立验收。完成这些后才可正式采用 UDP 候选并运行当前源码的完整质量链。

源码复核使用知识图谱，当前代际 2026-09-22T02:15:16Z；客户端 decltype 解析缺口已直接读取。新测试路径没有图覆盖，已读取全部实现，并由编译依赖和实际运行补证。证据目录在修改前保存了全部 12 份当前文档，历史记录没有覆盖。
