2026-09-24 终态更新：render-quit A 已 exit 1。Release 的单元、真实 TCP/UDP 窗口及 15322 帧独立回放通过；完整 Debug 的 10 项取消回归通过，但实际 TCP 窗口未在原 30 秒内开始比赛呈现，报 Actual graphical startup deadline，客户端与服务端均正常退出。2739 项固定输入和 28 份命令日志已核验。随后仅增加分段计时的 native-control-sanitized-starvation-diagnostic-20260924-a 也已 exit 1，复现同一启动超时；其分段数据仍待分析。两阶段均已终止，未通过双构建图形验收，候选尚未采纳。以下为历史记录。

2026-09-24 Release 图形验证通过：render-quit A 的 TCP/UDP 各完成 1003/1002 次实际呈现，退出为 73.241/105.959ms；正在绘制的帧分别在 Q 后 6.121/21.285ms 取消，均无交换且逻辑哈希不变，真实全局暂停/恢复标题与输入清理通过。7825+7497=15322 帧全部独立回放通过，15 份 Release 命令日志哈希已复核。完整 Debug/ASan/UBSan 当前执行中；正式源码未采纳，整体图形和产品验收仍未完成。以下为历史记录。

# 原生窗口退出：渲染检查点取消

归属 ms-23.1 → plan-23.1.1 → task-23.1.1.2，关联输入与渲染。正式源码未变，以下仅为私有候选。

GUI A 在 1003 次呈现、8505 确认帧后，暂停中 Q 到进程退出耗时 338.645ms，原 250ms 验收失败。短程和后续长程诊断均不能覆盖该失败。long B 两次 TCP、一次 UDP 的原 1000 呈现规模复测退出约 136.885/137.710/168.543ms，保存耗时约 10.149/14.398/14.748ms，环境关闭约 21.030/18.101/21.572ms；事件读取约 0.890/1.442/4.262ms，但游戏帧线程到退出边界约 89.448/89.544/113.210ms。独立回放全部通过，2724 项输入和 10 份命令日志哈希已复核。原始 339ms 的完整因果来源仍未证明。

候选在现有渲染检查点中读取线程安全的退出标志，通过私有取消信号中断尚未呈现的当前帧。只捕获自己的取消信号，其他引擎或输入异常继续传播。游戏线程继续执行既有传输停止、回放持久化和连接关闭，SDL 仍留在创建线程，最后正常销毁环境。现有帧队列清理与 Tracker/context 作用域守卫保留。该改动减少退出请求后剩余渲染工作，尚不能单凭实现宣称消除原超时。

另补全 Streaming 状态中的 Pausing/Paused by host/Resuming 提示，按已协商控制和实际游戏线程提交状态显示；恢复连接各阶段提示保持。

10 项回归覆盖完整绘制、预先退出、不完整帧资源析构、途中取消、渲染/输入异常透传、外层服务恢复、4ms采样节奏与线程隔离。Release 已通过，完整 Debug 及实际窗口仍执行中。实际窗口以 XTEST 在观察到当前帧开始后发送 Q，要求恰好一次取消、未交换该帧、逻辑哈希不变；读取实际窗口标题验证全局暂停与恢复反馈。原 1000 次呈现/权威帧、30 秒启动、250ms退出、300 秒独立回放和完整 sanitizer 参数均保留。

当前不能认定 GUI、Host 产品菜单、完整弱网/性能或整体产品验收通过。原始失败及各诊断的输入、日志与回放均保持不可变；后续修正使用新阶段。
