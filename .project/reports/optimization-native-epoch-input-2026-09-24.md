2026-09-24 更新：候选 B 因同一行 EXPECT_THROW 标签重复及 Run 辅助函数与 testing::Test::Run 冲突而编译失败。新候选 C 仅修正测试命名与排版，原实现和全部 30 项用例保留；Release 与完整 Debug Sanitizer 各通过 30 项 C++ 测试和 7020 个真实 Python 对照（2136 合法、4884 非法）。9 条命令日志及输入哈希已核对。未接入正式会话、能力协商、主机暂停、ACK 屏障，epoch_window_integration_verified 仍为 false。以下为历史记录。

# 原生输入周期隔离候选：实现与待验收项

2026-09-24 提交前状态更新：正式 B 已结束，AI 决策通过，架构、输入和战术集成失败，本轮公共 UDP 完整运行未执行。控制记录和输入周期候选均因前置依赖失败结束，未执行测试。内存诊断 A、B 已完成采集，未关闭原架构失败。独立 UDP 探针迁移报告通过 8 场对局和 16 次独立回放；真实客户端探针迁移报告通过 4 场对局和 8 次独立回放。上述探针仍在私有候选目录，尚未接入正式检查器，不能视为完整战术门禁或产品验收通过。以下保留阶段历史记录。

2026-09-24 正式架构门禁失败：重复重启生命周期检查未满足预热后 RSS 增长 ≤16 MiB 的原限制。正式失败记录与日志已归档，原阈值和消毒器配置未改变。B 驱动继续收集输入、战术及 AI 门禁结果，但本轮不会进入公共 UDP 实测；两个暂停相关候选仍排队，前置链终止后将以依赖失败结束。已另行排入保持原测试流程的内存诊断，等待现有链全部结束后执行。整体验收未完成，详见[生命周期内存诊断](../reports/optimization-lifetime-rss-2026-09-24.md)。


归属：ms-23.1 → plan-23.1.1 → task-23.1.1.2。状态：候选已写入独立目录并排队，未接入正式代码，未执行编译或运行验收。

## 实现

私有阶段 native-epoch-input-contract-20260924-a 增加周期输入编解码和固定输入窗口适配。记录遵循现有 Python kind=21 格式，保留小端浮点原始位；限制 1–22 个唯一席位、合法帧范围、有限方向值和动作位。完整席位掩码必须与已分配席位完全一致。解析采用固定大小存储，拒绝畸形记录后不会修改输入窗口。

窗口复用正式 ServerInputWindow 的 16 帧容量。暂停/恢复推进新周期时清除旧输入；旧周期有效包丢弃，非法旧周期包仍拒绝；未来周期、暂停中、尚未确认恢复的输入不进入窗口。重复运行提交不清除已接收的新输入。控制边界必须与窗口当前帧一致，最终帧不得回绕。宿主调用者仍须验证真实引擎边界和哈希，并在同一所有者线程或锁下调用。

## 验收准备

30 项 GTest 覆盖独立字节样例、22 席位、截断/后缀/种类/数量/浮点/动作位、重复和外部席位、暂停清理、恢复 ACK、旧周期、帧窗口、冲突包不部分写入和计数上界。对照程序直接调用现有 Python unpack_epoch_input、FrameInputWindow.validate 和 pack_epoch_input，并保留输入/期望/输出字节及哈希；覆盖合法记录、全部消息种类、完整多席位截断、非法配置及浮点边界。

执行顺序为正式接入 B 的全部门禁与公共入口运行 → 控制记录候选 A → 本候选 A。两个配置分别为 Release 与完整 Debug（-g、ASan/UBSan、禁止恢复、保留栈帧、不使用 -O1）。此时只有 Python 语法检查完成，测试数量是计划范围，不是通过结果。

## 源码审查发现

现有 C++ IsValidSlotInput 使用 1.001f，而 Python 使用双精度 1.001。最接近该界限且稍大的 float32 在两者的接受结果可能不同；新周期解码明确采用 Python 的双精度界限，并加入其前后相邻值用例。正式旧协议代码没有修改。候选窗口保留原生重复输入语义；跨语言对照目前验证编解码和归属校验，不代表全部服务端时序已等价。

图证据等级 Verify，项目 root-work_space-football，generation 2026-09-24T01:24:18Z；unpack_epoch_input 的直接调用者为 MatchServerRuntime._dispatch，精确源码确认旧周期校验与恢复确认要求。相关路径均查覆盖，server 1049 行解析缺口已直读；私有候选位于索引排除目录，采用直接读取。源码快照和哈希保存在 native-slot-group-evidence-20260924-o。

## 仍需完成

实际原生协议能力协商、主机命令权限、权威帧线程接入、暂停/恢复 ACK 屏障、暂停中的心跳与恢复、菜单交互、检查点与档案，以及真实 TCP/UDP 运行验收。不得将本组件候选视为主机暂停功能或产品级验收完成。
