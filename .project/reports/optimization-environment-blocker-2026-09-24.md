# 原生优化接入的 WSL 执行阻塞

2026-09-24 执行路径恢复并正式接入：常规 archlinux 启动入口仍超时，但系统发行版可执行命令；以 root 进入现有 archlinux 命名空间后，发行版、UID、工作目录、字体链接、原 1120 项源码及 19 个驱动输入均核验一致，未重启服务或改动系统配置。B 原生驱动已启动（PID 2671829 / start_ticks 69514044），完成前置证据核验并接入 17 个文件，正式源码清单为 1128 项，21 项验收器自测通过，完整门禁正在串行执行。环境阻塞已解除；正式验收及产品级状态仍待后续检查与实际对局结果。

当前工程未完成产品级验收。多席位/默认 UDP 候选及新增测试的既有通过记录保留，正式接入驱动尚未启动，正式源码未被本轮队列修改。

连续三个回合出现相同的 Linux 新命令执行阻塞：提交回合已有 Wsl/Service/0x8007274c 工具错误；随后接入准备回合仍不能启动；本回合对同一 Windows 就绪队列核验至终态。队列 PID 12796（UTC 启动时间 2026-09-23T22:07:45.9200692Z）已退出，600 秒内的 15 次 /usr/bin/true 尝试全部超时，readiness-exit.json 的退出码为 1，native_driver_launched=false。

另一个独立只读诊断调用官方 WslLaunch 接口，Windows 接受请求后，原生命令在 20 秒内仍无输出、未退出。它不能作为 Linux 执行成功证据。/proc 的只读观察没有显示内存或进程数量耗尽；这也不能证明 WSL 故障根因。未重启发行版或服务，未修改系统配置。

接入阶段：.project/optimization/benchmarks/native-multiseat-adoption-20260924-b。终态归档和阻塞审计：native-slot-group-evidence-20260924-l/terminal-proof 与 blocked-audit.json。菜单功能差异及剩余验收见[菜单功能对齐审查](optimization-native-menu-parity-2026-09-24.md)。

恢复条件为 WSL 能可靠执行新的 Linux 命令。恢复后先重新核验原 1120 项源码清单、候选、前置输入/产物及原生进程状态，再启动现有 B 接入驱动。不得重复启动旧队列，不能以 Windows 静态检查代替 Linux 编译、ASan/UBSan、实际窗口或独立引擎回放。整体验收仍须覆盖框架、架构、性能、手感、网络、渲染、AI，以及既定菜单、弱网、容量、训练和交付范围。
