# ms-20.1 / plan-20.1.1 状态与所有权验收

2026-09-09，真实本地验收通过；ms-20.1 尚有逻辑/快照及架构集成任务未完成。

已修复比赛逻辑与 ECS 查询同步、裁判人物及控球统计遗漏；本计划继续完成 GameEnv 独占上下文、析构与 close、嵌套上下文恢复、线程隔离、资源释放顺序、GUI 页面重复释放、字体和 SDL/EGL 引用管理、Python 关闭/重启兼容。不再修改宿主信号处理器和 C 随机数状态。

实际离屏运行还发现了历史着色器编译错误：IBL 整数常量、SSR 深度采样类型及空间。已修复并对齐 IBL uniform，添加链接状态检查和失败清理，修正程序缓存与真实绑定不一致。详见 [生命周期契约](../optimization/environment-lifetime.md) 和 [状态契约](../optimization/state-ownership.md)。

| 验收 | 当前结果 |
|---|---|
| 验收工具回归 | 29 项通过 |
| 原生 / Python 依赖边界 | 7 个原生目标、ELF 检查、独立目录加载通过 |
| 框架回归 | Debug 无 O3；421 C++、165 Python；零跳过 |
| 真实比赛确定性 | 2 个种子 × 2 个进程 × 1000 帧，含快照重演 |
| 状态一致性 | 8,190 条断言通过 |
| 生命周期 | 378 条断言通过，含并发、12 次重开、Python 绑定、EGL 双实例及失败重试 |
| 分配泄漏 | 独立 LeakSanitizer，未使用抑制列表，无泄漏报告 |
| 图形环境 | llvmpipe (LLVM 22.1.8, 256 bits)，320×180 实际非空且变化帧 |

生命周期的常规运行在预热后的 12 次重开过程中 RSS 保持稳定。完整 ASan/UBSan 插桩、桌面 SDL 流程、视觉效果质量及目标 GPU 帧时间仍待后续任务验收。上述软件渲染结果不代表 1080p/60fps 已达标。

前置测试发现并修复一条旧解说测试的随机失败：合法进球台词不一定含“GOAL”。现在冻结测试时钟，验证完整事件台词集合、冷却与历史记录。

最新比赛 hash：seed 42 `50bd9e009bf6d46a`，seed 43 `8e1eaab71e7b134a`；生命周期修改未改变比赛逻辑结果。

复现：

```bash
python3 .project/quality.py run task-20.1.1.2
python3 .project/quality.py verify plan-20.1.1
```

本次通过证据：`environment_lifetime-1788926800105985627.log`、`state_ownership-1788926798016919486.log`、`framework_regression-1788926771754819672.log`、`native_boundary-1788926664220205148.log`；均位于 `optimization/evidence/`，对应源码指纹由验收工具保存。后续源码变更会使相关证据过期并要求重跑。

下一任务为 `task-20.1.2.1`：事务快照恢复、渲染与逻辑推进隔离、是否渲染不改变逻辑结果。优化总目标继续执行，未提交或发布。
