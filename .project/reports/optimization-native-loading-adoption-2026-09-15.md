# 原生加载接入、运行验证与剩余缺陷（2026-09-15）

C 已完成两种构建的 42 个目标，Release / ASan 各 31 组加载契约通过，断言分别为 2180 / 2328，零跳过。永久场景与 socket 错误诊断已接入；B 的偶发 Peer send failed 尚未复现，不能视为根因已解决。

真实客户端八个场景均完成了预期功能：双客户端同局恢复、丢失恢复确认后的再次恢复、图形加载重连、按键保持与射门轻点清理、加载取消和认证座位释放。Release 四项时延均达标。ASan 两项取消分别耗时 660.7ms、339.6ms，服务绑定分别为 36.26s、30.65s，超过原定 250ms / 30s，因此运行报告 passed=false，functional_verified=true。测量驱动正常退出并不代表产品验收通过。

八次独立旧核心交叉回放全部通过，共 60,268 条断言；覆盖两种当前客户端构建和两种旧核心构建。回放验证未发现本轮状态或存档兼容回归。

正式门禁 C 的质量自检、原生边界、状态所有权、环境生命周期与模拟契约已通过，架构回归正在执行。框架门禁已失败：560 项 C++ 通过；Python 765 项与 305 个子测试通过，3 项真实图形测试因缺少 DISPLAY / SDL 窗口而失败，后续独立进程确定性步骤未执行。已安排隔离 X11 下完整重跑该门禁，不删除图形测试或修改原断言。GoogleTest 使用本机已有的 1.18.0，离线配置成功，没有下载依赖。

UDP 连接代际组件此前在 Release / ASan 各 35 项通过。新握手候选加入固定 64 字节报文、地址绑定 cookie、客户端绝对期限、128 项连接容量和关闭后的重放保护；28 项双构建测试及真实 UDP 丢包/旧包用例已编写，等待正式门禁终止后串行执行。该候选尚未接入产品 UDP 会话、FNRC 席位恢复或完整弱网矩阵。

下一项诊断使用当前正式源码的私有副本，记录客户端取消各阶段与服务端资源加载阶段，不改动原时延门槛或产品行为。依赖次序为：正式门禁 C → UDP 握手 A → 加载耗时诊断 → 隔离 X11 完整框架门禁 → UDP 握手 B。预检查发现本机 GoogleTest 只有共享库，A 已排队的不可变驱动错误要求静态库，将在编译前失败；B 保留相同候选与全部测试，修正为记录实际库文件。当前尚无 UDP 握手编译或测试通过证据。

本次进度与精确前版文档归档： [evidence E](../optimization/benchmarks/native-loading-adoption-evidence-20260915-e/progress.json)。全链仍有活动任务，因此未生成新的完整验收收据。历史性能基线 ad3a56a95f06a900b487253b9986e6f9064dd20dbb2f60e342a0efef43fb9a0c 的原文件仍缺失，两个历史子进程退出状态仍未知，均保留原限制。

本次框架失败终态、精确前版文档和固定测试 XML 已追加归档于 [evidence F](../optimization/benchmarks/native-loading-adoption-evidence-20260915-f/progress.json)。
