# UDP 恢复失败与线程退出契约

2026-09-24：正式完整框架 C 已通过：703 项 C++、783 项 Python 和 305 个子测试；两个种子各 1000 帧、各两个独立进程及快照回放一致。143 项完整 Debug ASan/UBSan 组件测试通过，当前 1117 项源码及产品哈希已核对。真实 UDP 恢复 C 的 Release 两组场景与独立回放通过；Debug、快照中断和真实窗口输入继续串行验收。完整弱网矩阵、默认 UDP 入口迁移、训练和产品级验收仍未完成。

以下保留此前阶段记录：

2026-09-24：完整框架 B 的 703 项 C++ 通过；Python 782 项通过，1 项恢复失败测试仍期望立即进入 gave_up，正式门禁失败。恢复、快照和图形输入阶段均因依赖失败结束，未执行产品场景。

本轮保留生产实现的 giving_up 阶段，修正并加强永久测试。通过真实 UDP 恢复和事件控制工作线程最后退出，确定性复现原断言失败；新测试要求线程存活期间持续 giving_up、1000 次 tick 小于 0.3 秒、无 Ready/成功/终态回调、快照与候选资源释放。释放线程后要求 gave_up，逻辑线程只通知一次，重试计数不变，错误原因保留。

定向验证 2 项通过；TCP/UDP 重连相关回归 46 项通过。将终态观察器变异为提前发布 gave_up 时，新测试确定性拒绝该行为。测试保留原 3 秒进度等待，未修改生产代码或放宽验收预算。

原测试、复现程序、变异验证和日志在 .project/optimization/benchmarks/udp-restore-drain-contract-20260924-a。完整门禁 C 及其串行恢复 C、快照 C、图形输入 B 将使用新目录执行。完整网络、训练、渲染和产品级验收尚未完成。
