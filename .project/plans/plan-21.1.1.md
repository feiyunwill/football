# plan-21.1.1 — 测量与热点

2026-09-22 当前 1090 项源码的完整框架门禁失败：560 项 C++ 通过，Python 780 通过、1 项最大快照 UDP 恢复失败；后续原生独立比赛未执行。新核心集成 D/恢复 C 因前置失败停止。原测试 10 次独立复现有 2 次失败，先观察到服务端重试耗尽；64 分片窗口候选又暴露握手超时，尚未采用。见[定位与完整失败证据](../reports/optimization-python-udp-snapshot-2026-09-22.md)。

以下保留此前阶段记录。

2026-09-22 已正式接入共用文件分块读取及 14 项永久原生回归，当前源码 1090 项。接入前 461 个资源及完整比赛轨迹等价；24 次重复启动中 Debug CPU 配对中位比值 0.3458/0.3167，Release 仍有波动。完整框架门禁已启动，旧 1089 项源码通过记录不代替本次验收。见[实现与证据边界](../reports/optimization-native-file-read-2026-09-22.md)。

以下保留此前阶段记录。

2026-09-22 共用文件读取候选完整通过：461 个资源、双构建边界与 8 次真实引擎轨迹/快照回放全部一致；两组 Debug 启动由 17.88/35.85 秒降至 12.42/4.76 秒。正在预检永久契约及 24 次交替启动测量，正式源码尚未接入，产品门禁状态不提升。见[完整证据](../reports/optimization-native-file-read-2026-09-22.md)。

以下保留此前阶段记录。

2026-09-22 文件读取候选已通过双构建 461 个资源的字节和边界检查，Debug 局部读取 CPU 从 70.49 秒降至 1.05 秒。完整引擎最后一组仍在验证；永久回归及 24 次交替启动对照已准备/排队，正式源码尚未接入，不提升产品状态。见[最新验证进度](../reports/optimization-native-file-read-2026-09-22.md)。

以下保留此前阶段记录。

2026-09-22 动画读取候选已完成双构建等价验证，但整体启动收益未证实，暂不采用。新的 12 次真实启动分段显示，ASE 网格/材质阶段中约 91%–97% 的 CPU 用于材质贴图加载；已启动共用 GetFile 分块读取候选，覆盖 461 个资源、13 项边界及真实引擎对照，结果待终态。见[实现、测量与边界](../reports/optimization-native-file-read-2026-09-22.md)。

以下保留此前阶段记录。

2026-09-22 当前源码剖析已完成：4 次未插桩对照、12 次分段测量均保持初始状态哈希。6 次 Debug 剖析耗时 11.75–48.45 秒，其中 4 次超过 30 秒；主要耗时是 CPU 执行，ASE 构建、角色初始化和动画加载仍需优化。已建立动画按行读取及分词移动候选，293 个实际文件与 Release 两个种子的引擎对照通过，Debug 尚待终态。见[具体实现和验证边界](../reports/optimization-native-animation-load-2026-09-22.md)。

以下保留此前阶段记录。

2026-09-22 启动可靠性续验：ASE 修复后的集成候选 C 在 Debug TCP/UDP 分别以 13.50/21.32 秒启动通过，但随后实际 UDP 恢复矩阵的一组 Debug 服务端超过原 30 秒而失败；另一组为 14.79 秒。因此启动质量仍未稳定达标。新的当前源码剖析 B 已排队，采用两个种子的未插桩核心对照及每种构建各三次分段测量，记录墙钟与线程 CPU，保留原 Debug、sanitizer 和产品期限。见[当前失败及测量方案](../reports/optimization-native-udp-recovery-2026-09-22.md)。

以下保留此前阶段记录。

2026-09-22 正式接入：ASE 修复及 10 项永久原生回归已采用，Release/完整 ASan+UBSan 各通过 554 条断言；完整框架门禁通过 560 项 C++、781 项 Python、305 个子测试及两种子的独立 1000 帧/快照回放。当前正式源码为 1089 项。性能统计与整个产品验收仍未完成，见[接入及完整证据](../reports/optimization-native-ase-adoption-2026-09-22.md)。

2026-09-22：顶点去重完成双构建模型/回放等价验证，但收益尚不足以采用；当前 CPU 诊断指向 ASE 解析。解析器边界与分配优化候选已复现旧缺陷，并通过双构建各 11 项契约、24 个真实资源与四组 1000 帧/500 帧回放对照，本轮实际启动均满足原 30 秒门槛。正式接入、永久回归与完整产品验收尚未执行；状态不提升。见[实现与证据](../reports/optimization-native-startup-2026-09-22.md)。

类型：plan

状态由 `python3 .project/quality.py status` 根据当前源码和验收证据计算。

验收条件：

- 子任务通过且本计划验收证据适用于当前源码

依赖：无

执行顺序：task-21.1.1.1、task-21.1.1.2

验收检查：match_benchmark、ecs_performance

执行：`python3 .project/quality.py run plan-21.1.1`

实现未就绪、检查失败、证据过期或子任务未通过时，不能完成。

2026-09-13 历史验收：此前冻结版本的两个子任务与本计划均通过，见[原生性能与回放报告](../reports/optimization-native-performance-replay-progress-2026-09-10.md)。新增验收输入后的长局正式链 A 未通过 CPU 统计要求；当前继续优化实际采样发现的到球时间估算循环，完成差分与比赛轨迹预检后执行原固定正式比较。以当前源码和质量运行器状态为准，见[最新执行证据](../reports/optimization-long-match-acceptance-2026-09-13.md)。

2026-09-13 控球扫描优化：按次持有的惰性轨迹缓存已实现，Release／ASan+UBSan 各通过 1,555,846 条断言，四次完整比赛与原始轨迹一致。正式依赖链 C 已启动，固定性能比较及后续容量、长局未通过前不提升任务状态，见[实现与预检证据](../reports/optimization-ai-trajectory-2026-09-13.md)。
